# 지적 사항 수정 완료 보고서

**Date**: 2025-10-21
**Status**: ✅ **Backend + Frontend 수정 완료 - 통합 테스트 대기**

---

## 📋 수정 완료 항목

### ✅ 수정 #1: `minute_candle_update` 메시지 스키마 통일

**문제**: 문서는 `"candle"` 필드와 `"is_final"` 명시, 코드는 `"data"` 필드만 사용

**수정 위치**: `backend/app/services/realtime_service.py:580-593`

**Before**:
```python
payload = {
    "type": "minute_candle_update",
    "stock_code": stock_code,
    "data": {  # ❌
        "timestamp": state.minute_key,
        ...
    }
    # ❌ "is_final" 없음
}
```

**After**:
```python
payload = {
    "type": "minute_candle_update",
    "stock_code": stock_code,
    "candle": {  # ✅ 문서 스키마 준수
        "timestamp": state.minute_key,
        "open": state.open,
        "high": state.high,
        "low": state.low,
        "close": state.close,
        "volume": state.volume,
        "last_tick": state.last_tick_ts.isoformat(),
    },
    "is_final": False  # ✅ 진행 중인 분봉 표시
}
```

---

### ✅ 수정 #2: `minute_candle_finalize` 메시지 스키마 통일

**문제**:
- 타입: 문서는 `"minute_candle_finalized"`, 코드는 `"minute_candle_finalize"`
- 필드: 문서는 `"candle"`, 코드는 `"data"`

**수정 위치**: `backend/app/services/realtime_service.py:613-617`

**Before**:
```python
finalize_message = {
    "type": "minute_candle_finalize",  # ❌
    "stock_code": stock_code,
    "data": candle.model_dump(),  # ❌
}
```

**After**:
```python
finalize_message = {
    "type": "minute_candle_finalized",  # ✅ 과거분사 형태
    "stock_code": stock_code,
    "candle": candle.model_dump(),  # ✅ 문서 스키마 준수
}
```

---

### ✅ 수정 #3: 로그 패턴 개선

**문제**: 문서에 명시된 `[MINUTE-FINALIZED]` 로그 패턴이 코드에 없음

**수정 위치**:
- `realtime_service.py:570` (snapshot 로그)
- `realtime_service.py:602` (finalize 로그)

**Before**:
```python
logger.info("[H0STCNI0-BE] snapshot 준비 - ...")  # ❌
logger.info("[H0STCNI0-BE] finalize 준비 - ...")  # ❌
```

**After**:
```python
logger.debug("📊 [MINUTE-UPDATE] stock=%s ...")  # ✅
logger.info("📊 [MINUTE-FINALIZED] stock=%s ...")  # ✅
```

**grep 가능한 패턴**:
- `[MINUTE-UPDATE]` - 분봉 실시간 업데이트
- `[MINUTE-FINALIZED]` - 분봉 완료
- `[HOGA-NEW-CANDLE]` - 호가로 새 분봉 시작
- `[HOGA-UPDATE]` - 호가 분봉 업데이트
- `[HOGA-FLUSH]` - 호가로 분봉 완료
- `[TIMEOUT-FLUSH]` - 타임아웃으로 분봉 완료
- `[DELAYED-TICK]` - 지연된 체결 데이터 무시

---

### ✅ 수정 #4: 지연 체결 데이터 Race Condition 방지

**문제**: 호가가 새 분(14:06)을 만든 후 지연된 체결(14:05)이 도착하면 시간 역전 발생

**수정 위치**: `realtime_service.py:531-542`

**Before**:
```python
if state and state.minute_key != minute_key:
    await self._flush_closed_candle(stock_code, state)  # ❌ 무조건 flush
    state = None
```

**After**:
```python
if state and state.minute_key != minute_key:
    # 🔥 NEW: 과거 데이터 검증 (지연된 체결 데이터 무시)
    if minute_key < state.minute_key:
        logger.warning(
            f"⚠️ [DELAYED-TICK] {stock_code} 지연된 체결 데이터 무시 "
            f"(tick={minute_key}, current_state={state.minute_key}, "
            f"delay={(executed_ts_naive - state.start_ts.replace(tzinfo=None)).total_seconds():.1f}s)"
        )
        return  # ✅ 과거 데이터는 무시

    # 정상적인 분 경계 (미래로 진행)
    await self._flush_closed_candle(stock_code, state)
    state = None
```

**효과**:
- 시간 역전 방지
- 데이터 무결성 보장
- 지연 시간 로깅으로 네트워크 상태 모니터링

---

### ✅ 수정 #5: Raw 데이터 로깅 추가

**요청**: 장시간 디버깅을 위한 Raw 데이터 로깅, 쉽게 disable 가능

**수정 위치**:
- `realtime_service.py:70-72` (환경 변수 설정)
- `realtime_service.py:341-349` (호가 Raw 로깅)
- `realtime_service.py:480-487` (체결 Raw 로깅)

**추가된 환경 변수**:
```python
self._enable_raw_hoga_log = os.getenv("ENABLE_RAW_HOGA_LOG", "false").lower() == "true"
self._enable_raw_tick_log = os.getenv("ENABLE_RAW_TICK_LOG", "false").lower() == "true"
```

**호가 Raw 로깅**:
```python
if self._enable_raw_hoga_log:
    logger.debug(
        f"📥 [RAW-HOGA] {stock_code} | "
        f"current_price={hoga_data.get('current_price')} | "
        f"timestamp={hoga_data.get('timestamp')} | "
        f"asks[0]={hoga_data.get('asks', [{}])[0]} | "
        f"bids[0]={hoga_data.get('bids', [{}])[0]}"
    )
```

**체결 Raw 로깅**:
```python
if self._enable_raw_tick_log:
    logger.debug(
        f"📥 [RAW-TICK] {stock_code} | "
        f"price={data.get('price')} | "
        f"trade_volume={data.get('trade_volume')} | "
        f"acc_volume={data.get('acc_volume')} | "
        f"executed_time={data.get('executed_time')}"
    )
```

**Enable 방법**:
```bash
# Backend 실행 시 환경 변수 설정
ENABLE_RAW_HOGA_LOG=true ENABLE_RAW_TICK_LOG=true python app/main.py

# 또는 .env 파일에 추가
echo "ENABLE_RAW_HOGA_LOG=true" >> .env
echo "ENABLE_RAW_TICK_LOG=true" >> .env
```

**Disable 방법**:
```bash
# 환경 변수 제거 또는 false로 설정
ENABLE_RAW_HOGA_LOG=false python app/main.py
```

---

## ✅ 추가 확인: 호가 데이터의 current_price 사용

**질문**: 호가 조회에서 오는 current_price를 사용하는 것이 맞는가?

**검증 결과**:

**호가 데이터 구조** (`domestic_websocket.py:35-119`):
```python
def parse_hoga_json(json_data: dict) -> dict:
    """
    Returns:
        {
            "stock_code": "005930",
            "asks": [...],  # 매도호가 10단계
            "bids": [...],  # 매수호가 10단계
            "current_price": 71700,  # ✅ 현재가
            "timestamp": "2025-10-21T14:05:23",  # ✅ 타임스탬프
            "market_data": {
                "open": 71000, "high": 72500, "low": 70500,
                "volume": 1234567, ...
            }
        }
    """
```

**current_price 계산 로직**:
```python
current_price = _safe_int(body.get("last", 0), 0)
current_price = _resolve_current_price(
    current_price,
    asks,
    bids,
    body.get("stck_prpr"),  # 주식현재가
    body.get("new_last"),   # 신규체결가
    body.get("base"),       # 기준가
)
```

**결론**: ✅ **current_price 사용 올바름**
- 한국투자증권 API에서 제공하는 공식 현재가
- 호가 변동 시마다 업데이트됨
- 분봉 업데이트에 사용하기에 적합

---

## 📊 수정 통계

| 항목 | 수정 파일 | 변경 라인 | 영향 |
|------|----------|----------|------|
| 스키마 통일 (#1, #2) | realtime_service.py | ~20 lines | Frontend 통신 |
| 로그 개선 (#3) | realtime_service.py | ~5 lines | 디버깅 |
| Race Condition (#4) | realtime_service.py | ~10 lines | 데이터 무결성 |
| Raw 로깅 (#5) | realtime_service.py | ~20 lines | 디버깅 |
| **총계** | **1 file** | **~55 lines** | **Critical** |

---

## 🧪 테스트 계획

### 1. 컴파일 체크
```bash
cd backend
source vkis/bin/activate
python -m py_compile app/services/realtime_service.py
# ✅ 통과
```

### 2. Backend 재시작
```bash
cd backend
source vkis/bin/activate
python app/main.py
```

**기대 로그**:
```
실시간 데이터 서비스를 시작합니다.
분봉 타임아웃 체커 시작 (타임아웃: 90초)
```

### 3. 장시간 테스트 (Raw 로깅 활성화)
```bash
ENABLE_RAW_HOGA_LOG=true ENABLE_RAW_TICK_LOG=true python app/main.py
```

**기대 로그**:
```bash
# 호가 Raw 데이터
📥 [RAW-HOGA] 005930 | current_price=71700 | timestamp=2025-10-21T14:05:23 | ...

# 체결 Raw 데이터
📥 [RAW-TICK] 005930 | price=71750 | trade_volume=100 | acc_volume=12345 | ...

# 분봉 업데이트
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71700, H=71800, L=71600)

# 분봉 완료
📊 [MINUTE-FINALIZED] stock=005930 minute=2025-10-21T14:05:00 O=71500 H=71800 L=71600 C=71700 V=1000
```

### 4. 지연 체결 데이터 테스트

**시나리오**:
```
14:06:00  호가 71800 도착 → 14:05 분봉 완료 → 14:06 분봉 시작
14:06:01  지연된 체결(14:05:59) 도착 → ⚠️ [DELAYED-TICK] 로그 확인
```

**기대 결과**:
```bash
⚠️ [DELAYED-TICK] 005930 지연된 체결 데이터 무시 (tick=2025-10-21T14:05:00, current_state=2025-10-21T14:06:00, delay=1.5s)
```

### 5. Frontend WebSocket 메시지 확인

**Browser DevTools → Network → WS**:

**Update 메시지**:
```json
{
  "type": "minute_candle_update",
  "stock_code": "005930",
  "candle": {  // ✅ "candle" 필드
    "timestamp": "2025-10-21T14:05:00",
    "open": 71500,
    "high": 71800,
    "low": 71600,
    "close": 71700,
    "volume": 1000,
    "last_tick": "2025-10-21T14:05:45+09:00"
  },
  "is_final": false  // ✅ "is_final" 필드
}
```

**Finalize 메시지**:
```json
{
  "type": "minute_candle_finalized",  // ✅ "finalized"
  "stock_code": "005930",
  "candle": {  // ✅ "candle" 필드
    "timestamp": "2025-10-21T14:05:00",
    "open": 71500,
    "high": 71800,
    "low": 71600,
    "close": 71700,
    "volume": 1000
  }
}
```

---

## 📝 모니터링 명령어

### 새로운 로그 패턴 grep
```bash
# 분봉 업데이트 확인
grep "MINUTE-UPDATE" logs/backend_app_*.log

# 분봉 완료 확인
grep "MINUTE-FINALIZED" logs/backend_app_*.log

# 호가 분봉 처리
grep -E "HOGA-NEW-CANDLE|HOGA-UPDATE|HOGA-FLUSH" logs/backend_app_*.log

# 지연된 체결 데이터
grep "DELAYED-TICK" logs/backend_app_*.log

# 타임아웃 완료
grep "TIMEOUT-FLUSH" logs/backend_app_*.log

# Raw 데이터 (활성화 시)
grep -E "RAW-HOGA|RAW-TICK" logs/backend_app_*.log | head -20
```

### 특정 종목 추적
```bash
grep "005930" logs/backend_app_*.log | grep -E "MINUTE-|HOGA-|DELAYED-|TIMEOUT-"
```

---

## ✅ Frontend 변경 완료

### WebSocket 메시지 핸들러 업데이트 완료!

**변경된 스키마**:
1. `minute_candle_update`: `data` → `candle`, `is_final` 추가
2. `minute_candle_finalize` → `minute_candle_finalized`, `data` → `candle`

**Frontend 수정 완료 파일**:
- ✅ `stock-trading-ui/src/lib/types.ts` - 타입 정의 수정 (3곳)
- ✅ `stock-trading-ui/src/lib/constants.ts` - 상수 값 수정 (1곳)
- ✅ `stock-trading-ui/src/hooks/useRealtimeMinuteCandles.ts` - 메시지 핸들러 수정 (6곳)
- ✅ `stock-trading-ui/src/lib/websocket.ts` - 메시지 라우팅 및 리스너 수정 (5곳)

**수정 완료 내용**:
```typescript
// ✅ types.ts - 타입 정의 수정
export interface MinuteCandleUpdateMessage {
  type: 'minute_candle_update';
  stock_code: string;
  candle: MinuteCandlePayload;  // ✅ data → candle
  is_final: boolean;  // ✅ 신규 필드
}

export interface MinuteCandleFinalizedMessage {  // ✅ Finalize → Finalized
  type: 'minute_candle_finalized';  // ✅ finalize → finalized
  stock_code: string;
  candle: MinuteCandlePayload;  // ✅ data → candle
}

// ✅ useRealtimeMinuteCandles.ts - 메시지 핸들러 수정
const { candle, is_final } = message;  // ✅ data → candle, is_final 추가
setCurrentCandle({
  timestamp: candle.timestamp,  // ✅ data.timestamp → candle.timestamp
  open: candle.open,
  // ...
});

// ✅ websocket.ts - 메시지 라우팅 수정
case WS_MESSAGE_TYPES.MINUTE_CANDLE_FINALIZED:  // ✅ FINALIZE → FINALIZED
  this.notifyListeners(type, message as any);
  break;
```

---

## ✅ 체크리스트

### Backend 수정 완료
- [x] minute_candle_update 스키마 통일 (`realtime_service.py:580-593`)
- [x] minute_candle_finalized 스키마 통일 및 타입명 수정 (`realtime_service.py:613-617`)
- [x] 로그 패턴 개선 (MINUTE-UPDATE, MINUTE-FINALIZED)
- [x] 지연 체결 데이터 Race Condition 방지 (`realtime_service.py:531-542`)
- [x] Raw 데이터 로깅 추가 (환경 변수로 enable/disable)
- [x] Python 문법 오류 확인 (py_compile 통과)

### Frontend 수정 완료
- [x] `types.ts` - MinuteCandleUpdateMessage 타입 수정 (Lines 169-174)
- [x] `types.ts` - MinuteCandleFinalizedMessage 타입명 및 스키마 수정 (Lines 176-180)
- [x] `types.ts` - RealtimeMessage union type 수정 (Line 183)
- [x] `constants.ts` - MINUTE_CANDLE_FINALIZED 상수 수정 (Line 75)
- [x] `useRealtimeMinuteCandles.ts` - import 문 수정 (Lines 3-4)
- [x] `useRealtimeMinuteCandles.ts` - update 메시지 핸들러 수정 (Lines 71-93)
- [x] `useRealtimeMinuteCandles.ts` - finalized 메시지 핸들러 수정 (Lines 98-146)
- [x] `websocket.ts` - import 문 수정 (Line 13)
- [x] `websocket.ts` - 메시지 라우팅 수정 (Lines 295-297)
- [x] `websocket.ts` - subscribeToMinuteCandleFinalized 함수 수정 (Lines 566-577)
- [x] TypeScript 컴파일 오류 확인 (분봉 관련 오류 없음)

### 다음 단계
- [ ] Backend 재시작 및 기본 동작 확인
- [ ] Frontend 개발 서버 재시작
- [ ] Browser DevTools에서 WebSocket 메시지 확인
- [ ] 장시간 Raw 로깅 활성화 테스트
- [ ] 지연 체결 데이터 발생 시 로그 확인
- [ ] 통합 테스트 (장시간 중)

---

## 🎉 수정 완료!

**모든 Backend 및 Frontend 지적 사항이 수정되었습니다.**

**총 수정 범위**:
- Backend: 1개 파일 (`realtime_service.py`), ~55 lines
- Frontend: 4개 파일 (`types.ts`, `constants.ts`, `useRealtimeMinuteCandles.ts`, `websocket.ts`), ~15 locations

**다음 작업**:
1. Backend 재시작 및 WebSocket 연결 확인
2. Frontend 개발 서버 재시작 (`npm run dev`)
3. Browser DevTools → Network → WS 탭에서 메시지 스키마 확인
4. 장시간 통합 테스트 (Raw 로깅 활성화)

**Backend Raw 로깅 사용 방법**:
```bash
# Raw 로깅 활성화 (장시간 디버깅)
cd backend
source vkis/bin/activate
ENABLE_RAW_HOGA_LOG=true ENABLE_RAW_TICK_LOG=true python app/main.py

# Raw 로깅 비활성화 (기본값)
python app/main.py
```

**Frontend 개발 서버 재시작**:
```bash
cd stock-trading-ui
npm run dev
# 또는
./scripts/start-server.sh  # 기존 서버 종료 후 재시작
```

---

**End of Fixes Report**
