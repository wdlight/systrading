# 실시간 분봉 캐시 연동 완성 구현 계획

**문서 ID**: `1019.update.impl.md`
**작성일**: 2025-10-19
**담당자**: Backend/Frontend Developer
**우선순위**: 🔥 **HIGH** (핵심 기능 완성)

---

## 📊 현황 분석

### ✅ 이미 구현된 부분 (85%)
1. **WebSocket 체결 데이터 파싱** - `domestic_websocket.py`
2. **분봉 집계 로직** - `MinuteCandleState` in `realtime_minute.py`
3. **Persistence Queue** - `minute_finalize_queue` 생성됨
4. **Persistence Worker** - Worker 코루틴 존재
5. **TradingService 캐시 저장 메서드** - `update_minute_candle` 구현됨
6. **Frontend 실시간 훅** - `useRealtimeMinuteCandles.ts` 작동 중
7. **Gap Filling** - REST API 기반 누락 구간 보충 작동

### ❌ 누락된 부분 (15%)
1. **Persistence Handler 주입** - `main.py`에 핸들러 연결 안 됨
2. **Frontend Finalize 구독** - 완성된 분봉 이벤트 미활용

### 🎯 구현 목표
**WebSocket으로 완성된 분봉을 자동으로 `kordata/` 캐시에 저장하여, REST API 의존도를 줄이고 실시간성을 향상시킨다.**

---

## 🚀 구현 계획

### Phase 1: Backend Persistence Handler 연결 (1시간)

#### Task 1.1: main.py 수정

**파일**: `/home/wide/projects/systrading/backend/app/main.py`

**위치**: Line 125 이후 (RealtimeDataService 생성 직후)

**추가 코드**:
```python
# ==========================================
# 분봉 Persistence Handler 연결
# ==========================================
from datetime import datetime
from app.services.trading_service import TradingService
from app.models.schemas import ChartCandle

# TradingService 인스턴스 생성
trading_service = TradingService(korea_invest_service)

# Persistence 콜백 함수 정의
async def minute_persist_callback(stock_code: str, candle: ChartCandle):
    """
    WebSocket으로 완성된 분봉을 kordata/ 캐시에 저장

    Args:
        stock_code: 종목 코드 (예: "005930")
        candle: 완성된 분봉 데이터
    """
    try:
        # candle.timestamp: "2025-10-19T14:35:00+09:00" 형식
        target_date = datetime.fromisoformat(candle.timestamp)

        # 캐시 파일 업데이트
        success = await trading_service.update_minute_candle(
            stock_code=stock_code,
            target_date=target_date,
            candle_data=candle
        )

        if success:
            logger.info(
                f"✅ [Persistence] 분봉 캐시 저장 성공: "
                f"{stock_code} {candle.timestamp} "
                f"(O:{candle.open} H:{candle.high} L:{candle.low} C:{candle.close} V:{candle.volume})"
            )
        else:
            logger.warning(f"⚠️ [Persistence] 분봉 저장 실패: {stock_code} {candle.timestamp}")

    except Exception as e:
        logger.error(
            f"❌ [Persistence] 분봉 저장 오류: {stock_code} {candle.timestamp} - {e}",
            exc_info=True
        )

# RealtimeDataService에 핸들러 주입
realtime_service.set_minute_persist_handler(minute_persist_callback)
logger.info("🔗 [Startup] 분봉 Persistence Handler 연결 완료")

# app.state에 trading_service 등록 (API 엔드포인트에서 사용)
app.state.trading_service = trading_service
```

**검증 포인트**:
- `realtime_service._minute_persist_handler`가 None이 아닌지 확인
- 서버 시작 로그에 "🔗 [Startup] 분봉 Persistence Handler 연결 완료" 출력

---

#### Task 1.2: 로깅 강화

**파일**: `/home/wide/projects/systrading/backend/app/services/realtime_service.py`

**위치**: Line 552-556 (persistence worker 내부)

**수정 전**:
```python
if self._minute_persist_handler:
    await self._minute_persist_handler(stock_code, candle)
else:
    logger.debug(f"분봉 persistence handler 미설정 - {stock_code} {candle.timestamp}")
```

**수정 후**:
```python
if self._minute_persist_handler:
    await self._minute_persist_handler(stock_code, candle)
else:
    logger.warning(
        f"⚠️ [Persistence Worker] Handler 미설정! "
        f"분봉이 캐시에 저장되지 않음 - {stock_code} {candle.timestamp}"
    )
```

**목적**: Handler 미설정 상태를 더 명확하게 경고

---

#### Task 1.3: 메트릭 추가 (선택사항)

**파일**: `/home/wide/projects/systrading/backend/app/services/realtime_service.py`

**위치**: `__init__` 메서드

**추가 코드**:
```python
# 메트릭 카운터
self._persistence_success_count = 0
self._persistence_fail_count = 0
```

**위치**: `_minute_persistence_worker` 내부 (Line 541-566)

**수정**:
```python
try:
    if self._minute_persist_handler:
        await self._minute_persist_handler(stock_code, candle)
        self._persistence_success_count += 1  # ✅ 성공 카운트
    else:
        logger.warning(...)
        self._persistence_fail_count += 1  # ❌ 실패 카운트
except Exception as e:
    self._persistence_fail_count += 1
    logger.error(...)
```

**메트릭 조회 API** (선택):
```python
# backend/app/api/debug.py (새 파일)
@router.get("/metrics/persistence")
async def get_persistence_metrics():
    return {
        "success_count": realtime_service._persistence_success_count,
        "fail_count": realtime_service._persistence_fail_count,
        "queue_size": realtime_service.minute_finalize_queue.qsize()
    }
```

---

### Phase 2: Frontend Finalize 구독 (30분)

#### Task 2.1: WebSocket 타입 확인

**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/types.ts`

**확인 항목**:
```typescript
// 이미 정의되어 있는지 확인
export interface MinuteCandleFinalizeMessage {
  type: 'minute_candle_finalize';
  stock_code: string;
  data: {
    timestamp: string;
    open: number;
    high: number;
    low: number;
    close: number;
    volume: number;
  };
}
```

**없으면 추가**

---

#### Task 2.2: WebSocket 구독 헬퍼 확인

**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/websocket.ts`

**확인 항목**:
```typescript
// 이미 정의되어 있는지 확인
export function subscribeToMinuteCandleFinalize(
  callback: (payload: MinuteCandleFinalizeMessage) => void
): () => void {
  wsManager.on(WS_MESSAGE_TYPES.MINUTE_CANDLE_FINALIZE, callback);
  return () => wsManager.off(WS_MESSAGE_TYPES.MINUTE_CANDLE_FINALIZE, callback);
}
```

**없으면 추가**

---

#### Task 2.3: useRealtimeMinuteCandles 확장

**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/hooks/useRealtimeMinuteCandles.ts`

**현재 상태**: `minute_candle_update` (진행 중 분봉)만 구독

**수정 계획**:
```typescript
import { useState, useEffect } from 'react';
import {
  subscribeToMinuteCandles,
  subscribeToMinuteCandleFinalize  // ✅ 추가
} from '@/lib/websocket';
import type { ChartCandle } from '@/lib/types';

export function useRealtimeMinuteCandles(stockCode: string, enabled: boolean = true) {
  const [currentCandle, setCurrentCandle] = useState<ChartCandle | null>(null);
  const [finalizedCandles, setFinalizedCandles] = useState<ChartCandle[]>([]);  // ✅ 추가

  useEffect(() => {
    if (!enabled || !stockCode) {
      setCurrentCandle(null);
      setFinalizedCandles([]);  // ✅ 추가
      return;
    }

    // 1. 진행 중인 분봉 구독 (기존)
    const unsubscribeUpdate = subscribeToMinuteCandles((message) => {
      if (message.stock_code !== stockCode) return;

      const { data } = message;
      setCurrentCandle({
        timestamp: data.timestamp,
        open: data.open,
        high: data.high,
        low: data.low,
        close: data.close,
        volume: data.volume,
      });
    });

    // 2. 완성된 분봉 구독 (신규) ✅
    const unsubscribeFinalize = subscribeToMinuteCandleFinalize((message) => {
      if (message.stock_code !== stockCode) return;

      const { data } = message;
      const finalizedCandle: ChartCandle = {
        timestamp: data.timestamp,
        open: data.open,
        high: data.high,
        low: data.low,
        close: data.close,
        volume: data.volume,
      };

      // 완성된 분봉을 배열에 추가 (중복 방지)
      setFinalizedCandles((prev) => {
        const exists = prev.some(c => c.timestamp === finalizedCandle.timestamp);
        if (exists) return prev;
        return [...prev, finalizedCandle];
      });

      // 현재 분봉이 완성된 것이면 초기화
      setCurrentCandle(null);
    });

    return () => {
      unsubscribeUpdate();
      unsubscribeFinalize();  // ✅ 추가
    };
  }, [stockCode, enabled]);

  return { currentCandle, finalizedCandles };  // ✅ 반환값 확장
}
```

---

#### Task 2.4: 차트 컴포넌트 수정

**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/components/trading/RealtimeCandlestickChart.tsx`

**수정 전** (추정):
```typescript
const realtimeCandle = useRealtimeMinuteCandles(stockCode, timeframe === 'minute');

const finalChartData = useMemo(() => {
  if (!realtimeCandle) return baseChartData;
  // ... 병합 로직
}, [baseChartData, realtimeCandle]);
```

**수정 후**:
```typescript
const { currentCandle, finalizedCandles } = useRealtimeMinuteCandles(
  stockCode,
  timeframe === 'minute'
);

const finalChartData = useMemo(() => {
  let mergedData = [...baseChartData];

  // 1. 완성된 분봉 병합 (finalize 이벤트로 받은 것들)
  if (finalizedCandles.length > 0) {
    finalizedCandles.forEach((finalizedCandle) => {
      const existingIndex = mergedData.findIndex(
        c => c.timestamp === finalizedCandle.timestamp
      );

      if (existingIndex >= 0) {
        // 기존 데이터 교체 (WebSocket이 더 최신)
        mergedData[existingIndex] = finalizedCandle;
      } else {
        // 새 분봉 추가
        mergedData.push(finalizedCandle);
      }
    });

    // 타임스탬프 정렬
    mergedData.sort((a, b) => a.timestamp.localeCompare(b.timestamp));
  }

  // 2. 현재 진행 중인 분봉 병합 (update 이벤트)
  if (currentCandle) {
    const lastCandle = mergedData[mergedData.length - 1];
    const currentMinute = currentCandle.timestamp.substring(0, 16);
    const lastMinute = lastCandle?.timestamp.substring(0, 16);

    if (lastMinute === currentMinute) {
      // 같은 분봉: 마지막 항목 교체
      mergedData = [...mergedData.slice(0, -1), currentCandle];
    } else {
      // 새로운 분봉: 추가
      mergedData = [...mergedData, currentCandle];
    }
  }

  return mergedData;
}, [baseChartData, currentCandle, finalizedCandles]);
```

**효과**:
- REST API 재호출 없이 완성된 분봉이 차트에 즉시 반영
- 진행 중 분봉과 완성된 분봉이 명확히 구분됨
- 타임스탬프 순 정렬 보장

---

### Phase 3: 테스트 및 검증 (2시간)

#### Task 3.1: 단위 테스트

**파일**: `backend/tests/services/test_realtime_persistence.py` (신규)

```python
import pytest
from datetime import datetime
from app.services.realtime_service import RealtimeDataService
from app.models.schemas import ChartCandle

@pytest.mark.asyncio
async def test_persistence_handler_called(mocker):
    """Persistence handler가 올바르게 호출되는지 테스트"""
    mock_handler = mocker.AsyncMock()

    # RealtimeDataService 인스턴스 생성 (간소화된 초기화)
    service = RealtimeDataService(...)
    service.set_minute_persist_handler(mock_handler)

    # 분봉 완성 시뮬레이션
    test_candle = ChartCandle(
        timestamp="2025-10-19T14:35:00+09:00",
        open=62000,
        high=62200,
        low=61900,
        close=62100,
        volume=1500
    )

    await service._enqueue_candle_for_persistence("005930", test_candle)

    # Worker가 처리할 때까지 대기
    await asyncio.sleep(0.1)

    # Handler 호출 확인
    mock_handler.assert_called_once_with("005930", test_candle)
```

---

#### Task 3.2: 통합 테스트 시나리오

**시나리오 1: WebSocket → 캐시 저장 확인**

1. **준비**:
   ```bash
   cd /home/wide/projects/systrading/backend
   rm -rf kordata/005930/20251019.json  # 기존 캐시 삭제
   ```

2. **실행**:
   ```bash
   # 백엔드 시작
   ./scripts/start_backend.sh

   # 로그 모니터링
   tail -f logs/minute_candle_samples.log
   ```

3. **WebSocket 체결 데이터 전송** (테스트용):
   - 실제 장 시간 대기 OR
   - 모의 체결 데이터 주입

4. **검증**:
   ```bash
   # 1분 후 캐시 파일 확인
   cat kordata/005930/20251019.json | jq '.[-1]'
   # 예상 출력: 최근 완성된 분봉 데이터
   ```

5. **로그 확인**:
   ```
   ✅ [Persistence] 분봉 캐시 저장 성공: 005930 2025-10-19T14:35:00+09:00 (O:62000 H:62200 L:61900 C:62100 V:1500)
   ```

---

**시나리오 2: 5분 공백 후 복귀**

1. **삼성전자 선택** → 분봉 차트 확인
2. **하이닉스로 전환** → 5분 대기
3. **다시 삼성전자 선택**
4. **검증**:
   - REST API `/api/chart/005930/minute/full` 호출 로그 확인
   - Gap fill이 트리거되지 않음 (WebSocket이 캐시를 이미 업데이트했으므로)
   - 또는 Gap이 작아서 빠르게 보충됨

---

**시나리오 3: Frontend Finalize 이벤트 확인**

1. **개발자 도구 열기** (Chrome DevTools)
2. **Console**에서 WebSocket 메시지 모니터링:
   ```javascript
   // WebSocket 메시지 가로채기 (임시 디버깅)
   const originalOnMessage = WebSocket.prototype.onmessage;
   WebSocket.prototype.onmessage = function(event) {
     const data = JSON.parse(event.data);
     if (data.type === 'minute_candle_finalize') {
       console.log('🎉 Finalize 수신:', data);
     }
     return originalOnMessage.apply(this, arguments);
   };
   ```

3. **1분 경과 후**:
   - Console에 "🎉 Finalize 수신: ..." 출력 확인
   - 차트가 REST 호출 없이 자동 업데이트되는지 확인

---

#### Task 3.3: 성능 테스트

**목표**: 고빈도 체결 데이터에서 persistence worker가 버틸 수 있는지 확인

**테스트 방법**:
```python
# backend/tests/load/test_persistence_load.py
import asyncio
from app.services.realtime_service import RealtimeDataService

async def test_high_frequency_persistence():
    """100 종목 × 1000 체결/분 시뮬레이션"""
    service = RealtimeDataService(...)

    # 100 종목에 대해 1000개 분봉 생성
    for stock_code in [f"{i:06d}" for i in range(100)]:
        for minute in range(1000):
            candle = generate_random_candle(stock_code, minute)
            await service._enqueue_candle_for_persistence(stock_code, candle)

    # Queue 소진까지 대기
    await asyncio.sleep(10)

    # 검증
    assert service.minute_finalize_queue.qsize() == 0
    assert service._persistence_success_count == 100000
```

**기대 결과**:
- Queue backlog < 50
- 평균 처리 시간 < 10ms/candle
- 메모리 증가 < 100MB

---

### Phase 4: 배포 및 모니터링 (1시간)

#### Task 4.1: 배포 전 체크리스트

- [ ] `main.py`에 persistence handler 주입 코드 추가됨
- [ ] 서버 재시작 시 "🔗 [Startup] 분봉 Persistence Handler 연결 완료" 로그 확인
- [ ] `_minute_persist_handler is None` 경고 로그 발생하지 않음
- [ ] 캐시 파일(`kordata/`) 쓰기 권한 확인
- [ ] Frontend `useRealtimeMinuteCandles` 훅에 finalize 구독 추가됨
- [ ] 차트 컴포넌트가 `finalizedCandles` 배열 사용
- [ ] 기존 기능 회귀 없음 (호가창, 워치리스트 정상 작동)

---

#### Task 4.2: 배포

```bash
# 1. 코드 커밋
cd /home/wide/projects/systrading
git add .
git commit -m "feat: WebSocket 분봉을 캐시에 자동 저장하도록 persistence handler 연결

- backend/app/main.py: TradingService 인스턴스 생성 및 핸들러 주입
- stock-trading-ui/src/hooks/useRealtimeMinuteCandles.ts: finalize 이벤트 구독
- stock-trading-ui/src/components/trading/RealtimeCandlestickChart.tsx: finalize 분봉 병합 로직

Closes #분봉-캐시-연동"

# 2. 백엔드 재시작
cd backend
./scripts/restart_backend.sh

# 3. 프론트엔드 재빌드 (필요 시)
cd ../stock-trading-ui
npm run build
pm2 restart stock-trading-ui
```

---

#### Task 4.3: 모니터링 설정

**모니터링 항목**:
1. **Persistence 성공/실패 비율**
   - 로그: `grep "Persistence" backend/logs/*.log | grep -c "성공"`
   - 메트릭: `/api/debug/metrics/persistence`

2. **Queue backlog**
   - `minute_finalize_queue.qsize()` 모니터링
   - Alert: size > 100

3. **캐시 파일 크기**
   ```bash
   watch -n 60 'du -sh backend/kordata/*/20251019.json'
   ```

4. **메모리 사용량**
   ```bash
   ps aux | grep uvicorn | awk '{print $6}'  # RSS (KB)
   ```

**Alert 설정** (선택):
```python
# backend/app/services/realtime_service.py
if self.minute_finalize_queue.qsize() > 100:
    logger.error(f"🚨 [Alert] Persistence queue backlog: {self.minute_finalize_queue.qsize()}")
    # 필요 시 Slack/Discord 알림
```

---

## 📝 검증 기준 (Definition of Done)

### ✅ Backend
- [ ] `main.py`에서 `realtime_service.set_minute_persist_handler()` 호출됨
- [ ] 분 경계 전환 시 `kordata/{종목코드}/{YYYYMMDD}.json` 파일이 자동 업데이트됨
- [ ] 로그에 "✅ [Persistence] 분봉 캐시 저장 성공" 출력됨
- [ ] "⚠️ Handler 미설정" 경고 발생하지 않음
- [ ] Persistence worker가 큐를 정상적으로 소비함 (qsize 증가 없음)

### ✅ Frontend
- [ ] `useRealtimeMinuteCandles`가 `finalizedCandles` 배열 반환
- [ ] `minute_candle_finalize` WebSocket 메시지 수신 시 배열 업데이트됨
- [ ] 차트가 REST API 재호출 없이 완성된 분봉 표시
- [ ] 종목 전환 시 `finalizedCandles` 초기화됨 (메모리 누수 없음)

### ✅ Integration
- [ ] 5분 공백 후 복귀 시 Gap fill이 최소화됨 (WebSocket 캐시 덕분)
- [ ] REST API `/api/chart/{code}/minute/full`이 WebSocket 갱신된 캐시 반환
- [ ] 실시간 분봉과 과거 분봉이 타임스탬프 순으로 정렬됨
- [ ] 중복 분봉 없음 (finalize와 update 충돌 없음)

---

## 🎯 성공 지표

### 정량적 지표
- **캐시 업데이트 지연**: < 1초 (분 경계 후)
- **Persistence 성공률**: > 99%
- **Queue backlog**: < 10 (평상시)
- **Frontend REST 호출 감소**: 80% ↓ (finalize 활용 시)

### 정성적 지표
- WebSocket만으로 분봉 차트가 실시간 업데이트됨
- 사용자가 REST API 폴링 없이도 최신 데이터를 확인할 수 있음
- 서버 재시작 후에도 캐시가 일관성 유지

---

## 🔧 트러블슈팅

### Issue 1: Handler 호출 안 됨

**증상**: 로그에 "Handler 미설정" 경고

**원인**: `main.py`에서 핸들러 주입 코드 누락 또는 순서 오류

**해결**:
```python
# main.py에서 순서 확인
realtime_service = RealtimeDataService(...)  # 1. 생성
realtime_service.set_minute_persist_handler(...)  # 2. 핸들러 설정
await realtime_service.start()  # 3. 시작
```

---

### Issue 2: 캐시 파일 업데이트 안 됨

**증상**: `kordata/` 파일 타임스탬프가 변하지 않음

**디버깅**:
```python
# trading_service.py:update_minute_candle에 로그 추가
logger.info(f"💾 캐시 저장 시도: {stock_code} {target_date}")
success = self.chart_cache_service._save_to_cache(...)
logger.info(f"💾 캐시 저장 결과: {success}")
```

**원인 가능성**:
- 파일 권한 문제 (`chmod 755 kordata/`)
- `target_date` 파싱 오류 (timezone 불일치)
- `candle_data` 타입 불일치

---

### Issue 3: Frontend finalize 이벤트 안 옴

**증상**: WebSocket에서 `minute_candle_update`는 오는데 `finalize`는 안 옴

**확인**:
```python
# realtime_service.py:_flush_closed_candle에서 브로드캐스트 확인
await self.connection_manager.broadcast({
    "type": "minute_candle_finalize",  # 타입 일치 확인
    "stock_code": stock_code,
    "data": candle.dict(),
})
logger.info(f"📢 Finalize 브로드캐스트: {stock_code} {candle.timestamp}")
```

**Frontend 확인**:
```typescript
// constants.ts
export const WS_MESSAGE_TYPES = {
  MINUTE_CANDLE_FINALIZE: 'minute_candle_finalize',  // 스펠링 일치 확인
};
```

---

### Issue 4: 메모리 누수

**증상**: `finalizedCandles` 배열이 계속 증가

**원인**: 종목 전환 시 초기화 안 됨

**해결**:
```typescript
// useRealtimeMinuteCandles.ts
useEffect(() => {
  // stockCode 변경 시 초기화
  setCurrentCandle(null);
  setFinalizedCandles([]);  // ✅ 이 줄 필수!

  // 구독 로직...
}, [stockCode, enabled]);
```

---

## 📚 참고 문서

- `docs/plan/mincandle/1018.impl.daily.plan.md` - 3일 구현 계획 (원본)
- `docs/plan/mincandle/1018.review01.md` - 리뷰 및 개선 제안
- `docs/plan/mincandle/2025-10-20.impl.realtime-minute-candle-improvements.md` - 최신 개선 계획
- `backend/app/services/realtime_service.py` - RealtimeDataService 구현
- `backend/app/services/trading_service.py` - TradingService 구현
- `stock-trading-ui/src/hooks/useRealtimeMinuteCandles.ts` - Frontend 실시간 훅

---

## 🚀 Next Steps (향후 개선)

1. **Persistence 재시도 로직**: 실패 시 exponential backoff로 재시도
2. **Finalize 기반 REST 폴링 최적화**: Finalize 이벤트 수신하면 폴링 중단
3. **LRU 캐시**: `minute_candle_state`에 LRU 적용하여 메모리 최적화
4. **메트릭 대시보드**: Grafana로 persistence 성공률/지연 시각화
5. **통합 테스트 자동화**: CI/CD에 분봉 시나리오 테스트 추가

---

**작성자**: Claude Code
**최종 수정**: 2025-10-19
**상태**: 🔥 Ready for Implementation
