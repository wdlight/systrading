# 백엔드 실시간 분봉 파이프라인 로그 포인트 정리

**문서 ID**: `1021.backend.mincandle.logging.md`  
**작성일**: 2025-10-21  
**대상 파일**: `backend/app/services/realtime_service.py`

---

## 1. `_tr_result_loop`

| 라인 | 로그 내용 | 비고 |
|-----|-----------|------|
| 250 | `logger.error(f"TR 결과 처리 중 오류: {str(e)}")` | 배치 수집 루프 오류 |

---

## 2. `_process_message_batch`

| 라인 | 로그 내용 | 비고 |
|-----|-----------|------|
| 278 | `logger.critical("!!! WebSocket 프로세스에서 치명적인 오류가 발생했습니다 !!!")` | 하위 프로세스 에러 |
| 279 | `logger.error(f"오류: {message.get('error')}")` | 상세 오류 |
| 280 | `logger.error(f"Traceback:\n{message.get('traceback')}")` | 스택 출력 |
| 293 | `logger.error(f"배치 처리 중 오류: {e}")` | 배치 처리 예외 |

---

## 3. `_handle_tick_data`

| 라인 | 로그 내용 | 비고 |
|-----|-----------|------|
| 455 | `logger.warning(f"체결 타임스탬프 파싱 실패: {executed_time} (stock: {stock_code})")` | 시간 파싱 오류 |
| 461 | `logger.debug(f"장 시간 외 체결 데이터 무시: {stock_code} (executed_time={executed_time})")` | 장 외 체결 무시 |
| 472 | `logger.warning(f"음수 체결량 감지 → 0으로 보정: {stock_code}, delta={trade_volume}")` | 음수 체결량 보정 |
| 482 | `self.minute_logger.debug("tick sample | stock={stock} minute={minute} payload={payload}", ...)` | 분봉 샘플 로그 (`minute_candle_samples.log`) |
| 527 | `logger.error(f"체결 데이터 처리 오류: {e}")` | 처리 예외 |

---

## 4. `_broadcast_minute_snapshot`

| 라인 | 로그 내용 | 비고 |
|-----|-----------|------|
| 532 | `logger.info("📡 [MinuteCandle] snapshot broadcast 준비 - stock=%s minute=%s ...")` | 스냅샷 전송 직전 |
| 557 | `logger.info("📡 [MinuteCandle] snapshot broadcast 완료 - stock=%s minute=%s")` | 스냅샷 전송 완료 |

---

## 5. `_flush_closed_candle`

| 라인 | 로그 내용 | 비고 |
|-----|-----------|------|
| 568 | `logger.info("🎉 [MinuteCandle] finalize broadcast 준비 - stock=%s minute=%s ...")` | 분봉 확정 직전 |
| 587 | `logger.info("🎉 [MinuteCandle] finalize broadcast 완료 - stock=%s minute=%s")` | 분봉 확정 완료 |
| 599 | `logger.warning(f"minute_finalize_queue 가득 참: {stock_code}, {candle.timestamp}")` | 큐 초과 경고 |

---

### 참고
- 위 로그 중 `self.minute_logger.debug`는 `logs/minute_candle_samples.log`에 기록되며, 나머지는 기본 백엔드 로그(`logs/backend_app_YYYY-MM-DD.log`)에 남습니다.
- `MinuteCandle` 관련 `logger.info`는 2025-10-21에 새로 추가한 로그이며, 현재 로그 파일에서 해당 키워드를 검색하면 브로드캐스트 실행 여부를 확인할 수 있습니다.
