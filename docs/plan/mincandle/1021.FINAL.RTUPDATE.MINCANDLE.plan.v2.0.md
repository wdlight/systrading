# H0STASP0 호가 데이터를 활용한 분봉 실시간 업데이트 구현 계획 v2.0

**Date**: 2025-10-21
**Version**: 2.0 (Final Implementation Plan)
**Status**: 📋 Ready for Implementation

---

## 📋 개요

H0STASP0 (호가 데이터)에 포함된 `current_price`와 `timestamp`를 활용하여 분봉을 실시간으로 업데이트합니다.

---

## 🎯 구현 목표

**현재 상태**:

- ✅ H0STASP0 구독 완료 (domestic_websocket.py:379-385)
- ✅ 호가 데이터 수신 및 파싱 완료 (domestic_websocket.py:473-515)
- ✅ RealtimeDataService에서 호가 처리 완료 (realtime_service.py:301-384)
- ❌ **분봉 업데이트는 안 됨** (현재는 orderbook_update만 브로드캐스트)

**목표**:

- ✅ 호가 데이터의 `current_price` 변경 시 현재 분봉 업데이트
- ✅ 분봉 완료 시 자동 저장 및 새 분봉 시작
- ✅ 실시간 분봉 스냅샷 브로드캐스트 (`minute_candle_update`)

---

## 📁 수정할 파일 (2개)

### 1️⃣ `backend/app/models/realtime_minute.py`

**변경 내용**: `MinuteCandleState`에 가격만으로 업데이트하는 메서드 추가

**추가 코드**:

```python
def apply_price_only(self, price: float, tick_ts: datetime) -> None:
    """
    호가 데이터처럼 거래량 없이 가격만 업데이트
    (현재가 변동 추적용)

    Args:
        price: 현재가
        tick_ts: 업데이트 시각

    Note:
        - high/low 자동 갱신
        - close 업데이트
        - volume은 변경 안 함 (호가에는 거래량 없음)
    """
    self.high = max(self.high, price)
    self.low = min(self.low, price)
    self.close = price
    # volume은 업데이트 안 함 (호가에는 거래량 없음)
    self.last_tick_ts = tick_ts
```

**위치**: Line 23 이후 (기존 `apply_tick` 메서드 바로 다음)

**기존 코드 참조**:

```python
# Line 17-22 (기존)
def apply_tick(self, price: float, volume: int, tick_ts: datetime) -> None:
    self.high = max(self.high, price)
    self.low = min(self.low, price)
    self.close = price
    self.volume += volume
    self.last_tick_ts = tick_ts
```

---

### 2️⃣ `backend/app/services/realtime_service.py`

#### 수정 A: `_handle_hoga_data` 메서드에 분봉 업데이트 호출 추가

**수정 위치**: Line 377-379 (호가 브로드캐스트 직후)

**기존 코드**:

```python
# Line 376-379
logger.info(f"📡 호가 데이터 브로드캐스트: {stock_code}")
await self.connection_manager.broadcast(broadcast_message)

self.metrics_collector.metrics.record_message_processed()
```

**변경 후**:

```python
# Line 376-384
logger.info(f"📡 호가 데이터 브로드캐스트: {stock_code}")
await self.connection_manager.broadcast(broadcast_message)

# 🔥 NEW: 호가 데이터로 분봉 업데이트
current_price = hoga_data.get("current_price")
timestamp_str = hoga_data.get("timestamp")

if current_price and current_price > 0 and timestamp_str:
    await self._update_minute_from_hoga(
        stock_code=stock_code,
        price=float(current_price),
        timestamp_str=timestamp_str
    )

self.metrics_collector.metrics.record_message_processed()
```

---

#### 수정 B: 새 메서드 추가 - `_update_minute_from_hoga`

**추가 위치**: Line 603 이후 (`_flush_closed_candle` 메서드 다음)

**추가 코드**:

```python
async def _update_minute_from_hoga(
    self,
    stock_code: str,
    price: float,
    timestamp_str: str
) -> None:
    """
    호가 데이터(H0STASP0)의 current_price를 이용한 분봉 업데이트

    Args:
        stock_code: 종목 코드 (예: "005930")
        price: 현재가 (예: 71700.0)
        timestamp_str: ISO 8601 타임스탬프 (예: "2025-10-21T14:05:23")

    Process:
        1. 타임스탬프 파싱 및 장 시간 체크
        2. 분봉 키 생성 (분 단위로 절삭)
        3. 분봉 상태 갱신 (거래량 없이 가격만)
        4. 실시간 스냅샷 브로드캐스트

    Note:
        - 호가는 거래량이 없으므로 volume=0 유지
        - 가격만 업데이트 (OHLC)
        - H0STCNI0 체결 데이터와 병행 사용 가능 (거래량은 체결에서만)
    """
    try:
        # 1. 타임스탬프 파싱
        from datetime import datetime
        try:
            tick_ts = datetime.fromisoformat(timestamp_str)
            if tick_ts.tzinfo is None:
                tick_ts = tick_ts.replace(tzinfo=KST)
        except ValueError:
            logger.warning(f"호가 타임스탬프 파싱 실패: {timestamp_str}")
            return

        # 2. 장 시간 체크
        from app.utils.trading_hours import TradingHoursManager
        tick_ts_naive = tick_ts.astimezone(KST).replace(tzinfo=None)
        if not TradingHoursManager.is_trading_hours(tick_ts_naive, include_extended=True):
            logger.debug(f"장 시간 외 호가 분봉 업데이트 무시: {stock_code}")
            return

        # 3. 분봉 키 생성 (분 단위로 절삭)
        minute_key = tick_ts.strftime("%Y-%m-%dT%H:%M:00")

        # 4. 분봉 상태 갱신 (거래량 없이 가격만)
        async with self.minute_state_lock:
            state = self.minute_candle_state.get(stock_code)

            # 새로운 분봉 시작?
            if state and state.minute_key != minute_key:
                await self._flush_closed_candle(stock_code, state)
                state = None

 
            # 분봉 상태 생성 또는 업데이트
            if not state:
            # _handle_tick_data가 분 경계를 처리하므로, 여기서는 state가 없거나 키가 다르면 새 분봉으로 간주
            if not state or state.minute_key != minute_key:
                # 새 분봉 생성 (호가로 시작)
                # 이 분기가 실행된다는 것은, 해당 분의 첫 데이터가 '체결'이 아닌 '호가'라는 의미
                state = MinuteCandleState(
                    minute_key=minute_key,
                    open=price,
                    high=price,
                    low=price,
                    close=price,
                    volume=0,  # 호가는 거래량 없음
                    start_ts=tick_ts,
                    last_tick_ts=tick_ts,
                )
                self.minute_candle_state[stock_code] = state
                logger.info(f"📊 [HOGA-NEW-CANDLE] {stock_code} {minute_key} 시작 (open={price})")
            else:
                # 기존 분봉 업데이트 (가격만)
                # 기존 분봉 가격 업데이트
                state.apply_price_only(price, tick_ts)
                logger.debug(f"📊 [HOGA-UPDATE] {stock_code} {minute_key} (close={price}, H={state.high}, L={state.low})")

        # 5. 실시간 스냅샷 브로드캐스트
        await self._broadcast_minute_snapshot(stock_code, state)

    except Exception as e:
        logger.error(f"호가 분봉 업데이트 실패 ({stock_code}): {e}", exc_info=True)
```

---

## 🔄 데이터 플로우 비교

### Before (현재 구현):

```
H0STASP0 수신 (domestic_websocket.py)
  ↓
parse_hoga_json() / receive_realtime_hoga_domestic_new()
  ↓
ws_result_queue.put(action_id='실시간호가')
  ↓
realtime_service._handle_hoga_data()
  ↓
orderbook_cache 저장
  ↓
broadcast(type='orderbook_update')
  ↓
❌ 분봉 업데이트 없음 ❌
```

### After (구현 후):

```
H0STASP0 수신 (domestic_websocket.py)
  ↓
parse_hoga_json() / receive_realtime_hoga_domestic_new()
  ↓
ws_result_queue.put(action_id='실시간호가')
  ↓
realtime_service._handle_hoga_data()
  ↓
orderbook_cache 저장
  ↓
broadcast(type='orderbook_update')
  ↓
✅ _update_minute_from_hoga() 호출 ✅
  ↓
MinuteCandleState.apply_price_only(price, tick_ts)
  ↓
_broadcast_minute_snapshot()
  ↓
broadcast(type='minute_candle_update') ✅
```

---

## 📊 실시간 업데이트 특징

### 장점:

1. ✅ **실시간성**: 호가 변경 즉시 분봉 반영 (초 단위)
2. ✅ **추가 구독 불필요**: 이미 H0STASP0 구독 중
3. ✅ **네트워크 효율**: 기존 데이터 활용
4. ✅ **가격 정확도**: 현재가 실시간 추적
5. ✅ **기존 인프라 활용**: 별도 백엔드 구현 없음

### 제한사항 및 해결책:
| 제한사항 | 영향 | 해결 방법 |
|---------|------|----------|
| 호가는 거래량 없음 | volume=0 유지 | H0STCNI0 체결 데이터와 병행 사용 (체결 시 거래량 추가) |
| 업데이트 빈도 높음 | 브로드캐스트 과다 | `_broadcast_minute_snapshot`는 이미 최적화됨 |
| 가격만 업데이트 | OHLCV 중 V 부족 | 거래량은 체결 데이터(H0STCNI0)로 보완 |

| 제한사항           | 영향              | 해결 방법                                              |
| ------------------ | ----------------- | ------------------------------------------------------ |
| 호가는 거래량 없음 | volume=0 유지     | H0STCNI0 체결 데이터와 병행 사용 (체결 시 거래량 추가) |
| 업데이트 빈도 높음 | 브로드캐스트 과다 | `_broadcast_minute_snapshot`는 이미 최적화됨           |
| 가격만 업데이트    | OHLCV 중 V 부족   | 거래량은 체결 데이터(H0STCNI0)로 보완                  |

---

## 🧪 테스트 시나리오

### 시나리오 1: 호가 변경 → 분봉 업데이트

**입력**:

```json
{
  "action_id": "실시간호가",
  "stock_code": "005930",
  "data": {
    "current_price": 71700,
    "timestamp": "2025-10-21T14:05:23",
    "asks": [...],
    "bids": [...]
  }
}
```

**예상 결과**:

1. `MinuteCandleState["005930"]["2025-10-21T14:05:00"]` 생성/업데이트
2. `state.close = 71700`
3. `state.high = max(기존 high, 71700)`
4. `state.low = min(기존 low, 71700)`
5. WebSocket 브로드캐스트:
   ```json
   {
     "type": "minute_candle_update",
     "stock_code": "005930",
     "candle": {
       "timestamp": "2025-10-21T14:05:00",
       "open": 71500,
       "high": 71800,
       "low": 71400,
       "close": 71700,
       "volume": 0,
       "is_final": false
     }
   }
   ```

**검증 로그**:

```
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71700, H=71800, L=71400)
```

---

### 시나리오 2: 분봉 경계 (14:05:59 → 14:06:00)

**입력**:

1. 14:05:59의 마지막 호가: `current_price=71750`
2. 14:06:00의 첫 호가: `current_price=71800`

**예상 결과**:

**Step 1** (14:05:59):

- 14:05 분봉 업데이트 (`close=71750`)

**Step 2** (14:06:00):

- 14:05 분봉 완료 (`_flush_closed_candle`)
- WebSocket 브로드캐스트:
  ```json
  {
    "type": "minute_candle_finalized",
    "stock_code": "005930",
    "candle": {
      "timestamp": "2025-10-21T14:05:00",
      "is_final": true,
      ...
    }
  }
  ```
- 14:06 새 분봉 시작 (`open=71800`)

**검증 로그**:

```
📊 [MINUTE-FINALIZED] 005930 2025-10-21T14:05:00 | O=71500 H=71800 L=71400 C=71750 V=0
📊 [HOGA-NEW-CANDLE] 005930 2025-10-21T14:06:00 시작 (open=71800)
```

---

### 시나리오 3: 체결(H0STCNI0) + 호가(H0STASP0) 혼합 사용

**타임라인**:

```
14:05:10 - 호가: current_price=71500 (분봉 시작, volume=0)
14:05:15 - 호가: current_price=71600 (가격 업데이트, volume=0)
14:05:20 - 체결: price=71550, volume=100 (거래량 추가!)
14:05:25 - 호가: current_price=71700 (가격 업데이트, volume=100 유지)
14:05:30 - 체결: price=71650, volume=50 (거래량 추가!)
```

**예상 분봉 상태**:

```json
{
  "timestamp": "2025-10-21T14:05:00",
  "open": 71500, // 첫 호가
  "high": 71700, // 호가 업데이트로 갱신
  "low": 71500, // 첫 호가
  "close": 71700, // 마지막 호가
  "volume": 150, // 체결 데이터로만 증가 (100 + 50)
  "open": 71500,    // 첫 호가
  "high": 71700,    // 호가 업데이트로 갱신
  "low": 71500,     // 첫 호가
  "close": 71700,   // 마지막 호가
  "volume": 150,    // 체결 데이터로만 증가 (100 + 50)
  "is_final": false
}
```

**핵심**:

- ✅ 가격은 호가로 실시간 추적 (초 단위)
- ✅ 거래량은 체결로만 증가 (정확성 보장)
- ✅ 정확한 OHLCV 분봉 생성

**검증 로그**:

```
📊 [HOGA-NEW-CANDLE] 005930 2025-10-21T14:05:00 시작 (open=71500)
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71600, H=71600, L=71500)
✅ [H0STCNI0-ENTRY] Processing execution/tick data  # 체결 발생
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71700, H=71700, L=71500)
✅ [H0STCNI0-ENTRY] Processing execution/tick data  # 체결 발생
```

---

## ⚙️ 설정 및 최적화

### 옵션 1: 로그 레벨 조정 (선택사항)

호가 업데이트가 너무 많으면 DEBUG로 변경:

**현재**:

```python
logger.info(f"📊 [HOGA-UPDATE] {stock_code} ...")
```

**변경 후**:

```python
logger.debug(f"📊 [HOGA-UPDATE] {stock_code} ...")
```

**적용 위치**: `realtime_service.py:_update_minute_from_hoga` 메서드 내부

---

### 옵션 2: 브로드캐스트 쓰로틀링 (선택사항)

초당 브로드캐스트 횟수 제한:

**추가 코드** (`realtime_service.py`):

```python
# Line 28-30 (__init__ 메서드 내부)
# 기존 코드
self.ws_result_queue = ws_result_queue
self.is_running = False
self.tasks = []

# 🔥 NEW: 브로드캐스트 쓰로틀링
self._last_hoga_broadcast: Dict[str, float] = {}
self._hoga_broadcast_interval = 0.5  # 0.5초마다 (초당 최대 2회)
```

**\_update_minute_from_hoga 메서드 수정**:
**_update_minute_from_hoga 메서드 수정**:

```python
async def _update_minute_from_hoga(...):
    # ... (기존 코드)

    # 4. 분봉 상태 갱신
    async with self.minute_state_lock:
        # ... (분봉 업데이트)

    # 🔥 NEW: 브로드캐스트 쓰로틀링 체크
    now = time.time()
    last_broadcast = self._last_hoga_broadcast.get(stock_code, 0)
    if now - last_broadcast < self._hoga_broadcast_interval:
        return  # 브로드캐스트 스킵 (너무 자주 호출됨)
    self._last_hoga_broadcast[stock_code] = now

    # 5. 실시간 스냅샷 브로드캐스트
    await self._broadcast_minute_snapshot(stock_code, state)
```

**효과**:

- 초당 브로드캐스트 2회로 제한 (0.5초 간격)
- 네트워크 트래픽 50% 감소
- Frontend 부하 감소

---

## 🚀 배포 및 모니터링

### 배포 순서:

1. **코드 변경 적용**

   ```bash
   # 2개 파일 수정
   - backend/app/models/realtime_minute.py
   - backend/app/services/realtime_service.py
   ```

2. **Backend 재시작**

   ```bash
   cd backend
   source vkis/bin/activate
   python app/main.py
   ```

3. **로그 모니터링**

   ```bash
   tail -f logs/backend_app_$(date +%Y-%m-%d).log | grep -E "HOGA-NEW-CANDLE|HOGA-UPDATE|MINUTE-FINALIZED"
   ```

4. **Frontend 동작 확인**
   - Browser DevTools → Network → WS
   - `minute_candle_update` 메시지 확인
   - 차트 실시간 업데이트 확인

---

### 모니터링 로그 패턴:

#### 정상 작동 로그:

```bash
# 새 분봉 시작
📊 [HOGA-NEW-CANDLE] 005930 2025-10-21T14:05:00 시작 (open=71500)

# 호가 업데이트 (자주 발생)
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71600, H=71600, L=71500)
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71650, H=71650, L=71500)
📊 [HOGA-UPDATE] 005930 2025-10-21T14:05:00 (close=71700, H=71700, L=71500)

# 분봉 완료
📊 [MINUTE-FINALIZED] 005930 2025-10-21T14:05:00 | O=71500 H=71700 L=71400 C=71650 V=0

# 다음 분봉 시작
📊 [HOGA-NEW-CANDLE] 005930 2025-10-21T14:06:00 시작 (open=71680)
```

#### 오류 로그 (문제 발생 시):

```bash
# 타임스탬프 파싱 실패
⚠️ 호가 타임스탬프 파싱 실패: invalid_timestamp

# 장 시간 외
장 시간 외 호가 분봉 업데이트 무시: 005930

# 예외 발생
❌ 호가 분봉 업데이트 실패 (005930): [error details]
```

---

### 모니터링 명령어:

```bash
# 1. 호가 분봉 시작 확인
grep "HOGA-NEW-CANDLE" logs/backend_app_*.log

# 2. 호가 분봉 업데이트 빈도
grep "HOGA-UPDATE" logs/backend_app_*.log | wc -l

# 3. 분봉 완료 확인
grep "MINUTE-FINALIZED" logs/backend_app_*.log

# 4. 브로드캐스트 확인
grep "minute_candle_update" logs/backend_app_*.log

# 5. 특정 종목 추적
grep "005930" logs/backend_app_*.log | grep -E "HOGA-|MINUTE-"

# 6. 오류 확인
grep -E "호가.*실패|호가.*오류" logs/backend_app_*.log
```

---

## 🔍 디버깅 가이드

### 문제 1: 분봉이 업데이트되지 않음

**체크리스트**:

1. ✅ H0STASP0 데이터 수신 확인

   ```bash
   grep "📡 호가 데이터 브로드캐스트" logs/backend_app_*.log
   ```

2. ✅ `current_price` 값 확인

   ```bash
   grep "current_price" logs/backend_app_*.log | head -5
   ```

3. ✅ `_update_minute_from_hoga` 호출 확인

   ```bash
   grep "HOGA-NEW-CANDLE\|HOGA-UPDATE" logs/backend_app_*.log
   ```

4. ✅ 장 시간 확인
   - 09:00-15:30 KST 내인지 확인

---

### 문제 2: 브로드캐스트 과다

**증상**: Frontend가 너무 많은 `minute_candle_update` 받음

**해결**:

- 옵션 2 (브로드캐스트 쓰로틀링) 적용
- `_hoga_broadcast_interval`을 1.0초로 증가

---

### 문제 3: 거래량이 0으로 표시됨

**원인**: 정상 동작 (호가는 거래량 없음)

**해결**:

- H0STCNI0 (체결 데이터) 활성화 필요
- 체결 발생 시 거래량 자동 증가
- 호가 + 체결 병행 사용

---

## 📝 코드 변경 요약

### 파일 1: `backend/app/models/realtime_minute.py`

**변경 내용**: 메서드 1개 추가

- `apply_price_only()` - 호가 데이터용 가격 업데이트

**코드 라인**: +7 lines

---

### 파일 2: `backend/app/services/realtime_service.py`

**변경 A**: `_handle_hoga_data()` 메서드 수정

- 호가 처리 후 분봉 업데이트 호출 추가
- **코드 라인**: +8 lines (Line 377-384)

**변경 B**: 새 메서드 추가

- `_update_minute_from_hoga()` - 호가 기반 분봉 업데이트 로직
- **코드 라인**: +60 lines

**총 변경**: +68 lines

---

## 🎯 기대 효과

### 실시간성 향상:

- **Before**: 1분마다 API 폴링 (최대 60초 지연)
- **After**: 호가 변경 즉시 반영 (최대 1초 지연)

### 네트워크 효율:

- **Before**: 1분마다 분봉 API 호출 (60회/시간)
- **After**: 기존 호가 데이터 활용 (0회 추가 호출)

### 사용자 경험:

- ✅ 차트가 실시간으로 움직임
- ✅ 현재가 변동이 즉시 반영
- ✅ 자연스러운 분봉 형성 과정 시각화

---

## 🔗 관련 문서

| 문서            | 경로                                                        | 내용                         |
| --------------- | ----------------------------------------------------------- | ---------------------------- |
| 현재 구현 분석  | `/docs/1021.CURRENT-IMPLEMENTATION.분봉-실시간-업데이트.md` | 기존 H0STCNI0 기반 구현 분석 |
| H0STCNI0 진단   | `/docs/1021.H0STCNI0.final-diagnosis.md`                    | H0STCNI0 동작 및 제약사항    |
| H0STACI0 디버그 | `/docs/1021.H0STACI0.debug-analysis.md`                     | WebSocket 승인 키 검증       |
| 문서 | 경로 | 내용 |
|------|------|------|
| 현재 구현 분석 | `/docs/1021.CURRENT-IMPLEMENTATION.분봉-실시간-업데이트.md` | 기존 H0STCNI0 기반 구현 분석 |
| H0STCNI0 진단 | `/docs/1021.H0STCNI0.final-diagnosis.md` | H0STCNI0 동작 및 제약사항 |
| H0STACI0 디버그 | `/docs/1021.H0STACI0.debug-analysis.md` | WebSocket 승인 키 검증 |

---

## ✅ 체크리스트

### 구현 전:

- [ ] 코드 리뷰 완료
- [ ] 테스트 시나리오 검토
- [ ] 백업 생성

### 구현:

- [ ] `realtime_minute.py` 수정 완료
- [ ] `realtime_service.py` 수정 완료
- [ ] 코드 포맷팅 확인

### 테스트:

- [ ] Backend 재시작 성공
- [ ] 로그에서 `HOGA-NEW-CANDLE` 확인
- [ ] 로그에서 `HOGA-UPDATE` 확인
- [ ] WebSocket 브로드캐스트 확인
- [ ] Frontend 차트 업데이트 확인

### 배포:

- [ ] 장 시간 중 테스트 완료
- [ ] 성능 모니터링 완료
- [ ] 문서 업데이트

---

## 🚧 주의사항

1. **장 시간 중 배포**

   - 가능하면 장 종료 후 배포
   - 장중 배포 시 빠른 롤백 준비

2. **거래량 표시**

   - 호가만 사용 시 volume=0으로 표시됨
   - H0STCNI0 체결 데이터와 병행 권장

3. **브로드캐스트 빈도**

   - 호가 변경이 매우 자주 발생할 수 있음
   - 필요 시 쓰로틀링 적용

4. **메모리 관리**
   - `minute_candle_state`는 TTL=10분 자동 정리
   - 메모리 누수 없음

---

**End of Plan v2.0**
