# 2025-10-21 Codex 작업 로그

## 프론트엔드
- `useRealtimeMinuteCandles`:
  - 구독 전 단계 로그(`⚙️`)와 리스너 등록 완료 로그(`📡`)를 추가해 훅 실행 여부를 확인.
  - finalize 배열 크기 제한(600) 등 상태 유지 로직 정리.
- `useTRViewChart`:
  - REST 데이터 로드 → 실시간 병합 흐름을 확인하기 위한 디버그 로그(`📡`, `🔁`, `📈`) 보강.
- `OrderBook`/`websocket.ts`:
  - 과도한 호가 로그를 주석 처리하고, WebSocket 받는 메시지는 타입별로 이미 샘플링 로그(`📡 WebSocket [received] …`)가 출력되는 구조 확인.
- 일봉/분봉 REST 호출이 정상 수행되고 있음을 콘솔 로그로 검증.

## 백엔드
- `domestic_websocket.py`:
  - 한국투자증권 체결 채널(H0STCNI0) 수신 payload를 `[H0STCNI0] raw payload` / `[H0STCNI0] parsed data` 태그로 로그 출력하도록 추가.
- `realtime_service.py`:
  - `_broadcast_minute_snapshot`, `_flush_closed_candle`에 `[H0STCNI0-BE] …` 로그를 추가해 백엔드에서 프론트로 분봉 메시지가 전송되는지 추적 가능하도록 함.
  - 기존 `_handle_tick_data` 경로의 로그 위치를 문서화하여 흐름 분석용 참고 문서 작성 (`docs/plan/mincandle/1021.backend.mincandle.logging.md`).

## 분석 & 문서
- `docs/plan/mincandle/1021.analysis.backend.mincandle.md` 확인: 파이프라인 설명이 실제 코드와 일치함을 검증.
- 새로운 로그 포인트 정리 문서 `1021.backend.mincandle.logging.md` 작성.
- 기존 진단 문서(`1021.trview.websocket.diagnosis.md`, `1021.trview.realtime-candle-not-working.final-diagnosis.md`)의 미비점 파악 및 추가 검증 방향 명시.

## 로그 확인
- `logs/backend_app_2025-10-21.log`에서 `[H0STCNI0-BE]`나 분봉 관련 로그가 없는 것을 확인. → 체결 메시지가 백엔드에 도달하지 않았거나 장 외 시간엔 분봉 브로드캐스트가 발생하지 않는 상황으로 추정.
- 장중 재현 시 새로 추가한 로그로 H0STCNI0 수신 → 분봉 브로드캐스트까지 전체 흐름을 추적할 계획.

## 다음 단계 제안
1. 장중에 새로 추가한 로그들을 기반으로 체결 메시지 도달 여부부터 확인.
2. 백엔드 큐(`ws_result_queue`)에 체결 메시지가 쌓이는지 `_tr_result_loop` 경로에 임시 로그 추가 고려.
3. 필요 시 프론트에서 `minute_candle_update` 수신 여부를 `📡 WebSocket [received] …` 로그로 재확인.
