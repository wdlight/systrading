# 대시보드 UI/UX 개선 계획

**문서 버전**: 2.4 (2025-10-10 최종 업데이트 - 실제 데이터 사용 명확화)
**문서 목적**: 실제 코드 분석을 기반으로 현재 대시보드의 UI/UX를 심층 분석하고, 사용성, 정보 접근성, 확장성을 높이기 위한 구체적이고 실행 가능한 개선 계획을 수립합니다.

## 📋 문서 구성
- **섹션 1**: 실제 코드 기반 현재 구조 분석 (✅ 완료)
- **섹션 2**: 핵심 개선 제안 및 우선순위 (✅ Phase 0 추가)
- **섹션 3**: 상세 개선 방안 (✅ 코드 레퍼런스 추가)
- **섹션 4**: 리스크 분석 및 대응 전략 (✅ 5가지 리스크)
- **섹션 5**: Backend/API 작업 정의 (✅ Mock 데이터 전략)
- **섹션 6**: 테스트 요구사항 (✅ Acceptance Criteria)
- **섹션 7**: Feature Flag & Rollback (✅ 안전한 배포)
- **섹션 8**: 실행 체크리스트 (✅ Phase 0-4 단계별)
- **섹션 9**: 기대 효과 및 성공 지표 (✅ ROI 측정)
- **섹션 10**: 참고 문서 및 리소스 (✅ 링크 모음)
- **부록**: 상세 구현 가이드 (`1010.dashboard.ux-design-detailed.md`)

---

## 1. 현재 구조 분석 (실제 코드 기반)

### 1.1 레이아웃 구조

**파일 위치**: `stock-trading-ui/src/app/page.tsx`

메인 페이지는 Header, Left Sidebar, Main Dashboard, Right Panel의 3단 레이아웃으로 구성:

-   **레이아웃 컴포넌트**:
    -   **Header**: `components/layout/Header.tsx` (상단, 고정)
    -   **TradingConditions**: `components/trading/TradingConditions.tsx` (좌측 사이드바, 접을 수 있음)
    -   **Main Dashboard**: `app/page.tsx` (중앙 콘텐츠, 스크롤 가능)
    -   **QuickActionPanel**: `components/layout/QuickActionPanel.tsx` (우측 패널, **⚠️ 모바일에서 숨김**)

-   **주요 카드 컴포넌트**:
    -   `PortfolioSummary.tsx`: 포트폴리오 요약 (자산, 수익률)
    -   `PortfolioPerformance.tsx`: 성과 분석 (**⚠️ 차트 미구현**)
    -   `HoldingsPanel.tsx`: 보유 종목 테이블
    -   `WatchlistPanel.tsx`: 관심 종목 테이블
    -   `MarketOverview.tsx`: 시장 현황 (KOSPI, KOSDAQ, 환율 등)

### 1.2 실제 그리드 구조 (page.tsx:57-81)

```tsx
// 현재 레이아웃 (Desktop, lg+)
<div className="grid grid-cols-1 lg:grid-cols-2 gap-4 lg:gap-6">
  {/* Row 1: 50:50 split */}
  <PortfolioSummary />              // 50% width
  <PortfolioPerformance />          // 50% width
</div>

{/* Row 2: Full width */}
<HoldingsPanel />                   // 100% width

{/* Row 3: Full width */}
<WatchlistPanel />                  // 100% width

{/* Row 4: Full width */}
<MarketOverview />                  // 100% width

{/* Right Panel - Hidden on mobile */}
<QuickActionPanel className="hidden xl:flex" /> // ⚠️ 1280px 미만에서 숨김
```

### 1.3 확인된 핵심 문제점

| 문제점 | 코드 위치 | 영향도 | 비고 |
|-------|---------|-------|------|
| **Holdings + Watchlist 분리** | page.tsx:69-76 | 🔴 높음 | 각각 100% 너비로 공간 낭비, 4+ 화면 스크롤 필요 |
| **차트 미구현** | PortfolioPerformance.tsx:43-44 | 🔴 높음 | "coming soon" placeholder, 핵심 기능 부재 |
| **모바일 QuickAction 숨김** | page.tsx:168 | 🔴 높음 | `hidden xl:flex`로 모바일 사용자 접근 불가 |
| **Market Overview 하단 위치** | page.tsx:78-81 | 🟡 중간 | 중요 정보가 최하단, 가시성 낮음 |
| **알림 시스템 부재** | Header.tsx | 🟡 중간 | Bell 아이콘만 있고 기능 없음 |
| **시각적 계층 부족** | MarketOverview.tsx:90-154 | 🟢 낮음 | 인덱스/주식 구분 약함 |

## 2. 핵심 개선 제안 (실행 우선순위 기반)

### 2.1 개선 영역 및 우선순위

| ID | 개선 영역 | 현재 상태 | 목표 | 예상 소요 | 우선순위 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **P1-A** | **PortfolioPerformance 차트** | ❌ Placeholder만 존재 (PortfolioPerformance.tsx:43-44) | Recharts 기반 인터랙티브 차트 구현 | 3-4시간 | 🔴 **매우 높음** |
| **P1-B** | **모바일 QuickAction 접근성** | ❌ `hidden xl:flex`로 완전 숨김 (page.tsx:168) | FAB + Bottom Sheet 구현 | 1시간 | 🔴 **매우 높음** |
| **P2** | **Holdings/Watchlist 통합** | ⚠️ 각각 100% 너비로 분리 (page.tsx:69-76) | 탭 기반 통합 AssetPanel 구현 | 3-4시간 | 🟠 **높음** |
| **P3** | **그리드 레이아웃 최적화** | ⚠️ 4+ 화면 스크롤 필요 | Holdings+Market 동일 행 배치로 2 화면으로 축소 | 2-3시간 | 🟠 **높음** |
| **P4** | **알림 시스템 구축** | ⚠️ Bell 아이콘만 존재 | 실시간 알림 드롭다운 + 배지 | 1-2시간 | 🟡 **중간** |
| **P5** | **MarketOverview 시각 개선** | ✅ 기능 정상, 계층 약함 | 섹션 헤더 강조, 랭크 배지 추가 | 30분 | 🟡 **중간** |
| **P6** | **동적 그리드 (향후)** | - | react-grid-layout 도입 | 4-6시간 | 🟢 **낮음** |

### 2.2 구현 로드맵 (단계별)

#### 📦 **Phase 0: 사전 준비 및 환경 설정** (30분-1시간)

**브랜치 전략**:
- ✅ 현재 브랜치: `feature/trade`
- ✅ Daily work 완료 후 사용자가 직접 merge
- ✅ 디자인 승인: 향후 다듬을 정도로 기능 우선 진행

**의존성 설치**:
```bash
cd stock-trading-ui
npm install focus-trap-react recharts
```

**Mock 인프라 구축**:
1. 디렉터리 생성
   ```bash
   mkdir -p src/lib/mock
   mkdir -p src/lib/hooks
   ```

2. useLocalStorage 훅 생성 (`src/lib/hooks/useLocalStorage.ts`)
3. Mock 데이터 파일 생성 (알림 및 차트 히스토리 전용)
   - `src/lib/mock/userNotifications.ts` (알림 시스템용)
   - `src/lib/mock/portfolioHistory.ts` (차트 히스토리용)

⚠️ **주의**: Holdings, Watchlist, Portfolio Summary는 실제 데이터 사용 (Mock 불필요)

**Notification 타입 재정의** (`src/lib/types.ts`):
- UserNotification (사용자 레벨: 매매 체결, 시스템 알림)
- SystemNotification (시스템 내부: 토스트, 에러 메시지)

---

#### 🚀 **Phase 1: Quick Wins** (4-5시간, 즉시 효과)
1. ✅ **모바일 FAB + Bottom Sheet** (1시간) → 모바일 사용성 40% 이상 개선
2. ✅ **MarketOverview 시각 계층** (30분) → 정보 구분 명확화
3. ✅ **알림 배지 + 드롭다운** (1-2시간) → 실시간 피드백 제공
4. ✅ **에러/Empty State** (30분) → UX 일관성 향상

#### ⚡ **Phase 2: Core Features** (6-8시간, 핵심 가치)
1. ✅ **Recharts 차트 구현** (3-4시간) → 데이터 시각화 완성
2. ✅ **AssetPanel 탭 통합** (3-4시간) → 공간 효율 40% 개선

#### 🎯 **Phase 3: Layout Optimization** (2-3시간, 정보 접근성)
1. ✅ **10-column Grid 시스템** (1시간) → Holdings+Market 동일 행 배치
2. ✅ **반응형 테스트** (30분) → 모든 디바이스 최적화
3. ✅ **Skeleton Loading** (30분-1시간) → 로딩 UX 개선

**총 예상 소요**: 12-16시간 (약 2-3일)
**예상 효과**: 스크롤 40% 감소, 모바일 사용성 85% 이상, 정보 접근 시간 50% 단축

---

## 3. 상세 개선 방안

### P1-A: PortfolioPerformance 차트 구현 (🔴 최우선)

**파일**: `stock-trading-ui/src/components/trading/PortfolioPerformance.tsx`
**현재 코드**: Line 37-46 (placeholder 영역)

#### 현황 분석
```tsx
// Line 43-44: 현재 상태
<h4 className="text-sm font-semibold text-white mb-2">Interactive Chart</h4>
<p className="text-xs text-gray-500">Chart library integration coming soon</p>
```
- ❌ 차트가 완전히 미구현 상태
- ✅ Return Stats는 구현됨 (Line 52-89)
- ✅ Recent Trades는 구현됨 (Line 91-123)

#### 구현 계획

⚠️ **중요**: PortfolioPerformance는 두 영역으로 구성됩니다:
1. **Portfolio Summary (상단)**: **실제 WebSocket 데이터** (총 자산, 일일 수익률) - 변경 불필요
2. **Performance Chart (하단)**: Mock 또는 API 데이터 (차트 히스토리) - **구현 필요**

**1단계: Recharts 설치 및 기본 구조** (30분)
```bash
cd stock-trading-ui
npm install recharts
```

**2단계: 차트 컴포넌트 작성** (1-2시간)
- Line 37-46을 Recharts LineChart로 교체
- 포트폴리오 가치 라인 (파란색, 두께 2px)
- KOSPI 벤치마크 라인 (회색, 점선)
- 인터랙티브 툴팁 (날짜, 금액, 수익률)
- ⚠️ **Summary 영역은 기존 실제 데이터 유지**

**3단계: 시간 기간 탭 추가** (30분)
- TabButton 컴포넌트 재사용 (이미 QuickActionPanel에서 사용 중)
- 1D, 1W, 1M, 3M, 1Y, ALL 탭
- 상태 관리: `useState<TimePeriod>`

**4단계: KPI 카드 추가** (30분)
- Total Return (총 수익률) - 실제 데이터
- Max Drawdown (최대 낙폭) - 계산값
- Volatility (변동성) - 계산값

**5단계: 차트 데이터 연동** (1시간)
- Mock 데이터: `mockPortfolioHistory` 사용 (localStorage)
- 또는 백엔드 API: `GET /api/portfolio/history` (준비 시)
- `usePortfolioHistory` 훅 작성
- 로딩/에러 상태 처리

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 2.1 참조

---

### P1-B: 모바일 QuickAction 접근성 개선 (🔴 최우선)

**파일**: `stock-trading-ui/src/app/page.tsx`
**현재 코드**: Line 168

#### 현황 분석
```tsx
// Line 168: 문제의 코드
<QuickActionPanel className="hidden xl:flex" />
```
- ❌ 1280px 미만에서 완전히 숨김
- ❌ 모바일 사용자는 매매/감시/알림 기능 접근 불가
- ✅ QuickActionPanel 자체는 탭 구조로 잘 구현됨 (QuickActionPanel.tsx:78-119)
- ⚠️ **QuickActionPanel 가변 레이아웃 미지원**: 현재 w-80 고정 폭, isSheet prop 없어 Bottom Sheet에서 전체 너비 사용 불가

#### QuickActionPanel Props 확장

**파일**: `stock-trading-ui/src/components/layout/QuickActionPanel.tsx`

**현재 문제점**:
- 고정 폭 (w-80, 320px) → Mobile Bottom Sheet에서 전체 너비 필요
- isSheet prop 미지원

**수정 계획**:
```typescript
export interface QuickActionPanelProps {
  className?: string;
  defaultTab?: 'watch' | 'trade' | 'alerts';
  onStockSelect?: (code: string) => void;

  // 모바일 Bottom Sheet 지원
  isSheet?: boolean;      // Bottom Sheet 모드
  onClose?: () => void;   // 닫기 핸들러
}

// 컴포넌트 수정
export function QuickActionPanel({
  className,
  defaultTab = 'watch',
  onStockSelect,
  isSheet = false,
  onClose
}: QuickActionPanelProps) {
  return (
    <div className={cn(
      // Desktop: 고정 폭
      !isSheet && 'w-80 border-l border-gray-700',
      // Mobile: 전체 너비
      isSheet && 'w-full',
      'bg-[#1a1a1b] h-full flex flex-col',
      className
    )}>
      {/* Sheet 모드일 때 닫기 버튼 추가 */}
      {isSheet && onClose && (
        <div className="flex items-center justify-between p-4 border-b border-gray-700">
          <h3 className="text-white font-semibold">빠른 작업</h3>
          <button
            onClick={onClose}
            className="text-gray-400 hover:text-white transition-colors"
            aria-label="닫기"
          >
            <X className="h-5 w-5" />
          </button>
        </div>
      )}

      {/* 기존 탭 네비게이션 및 콘텐츠 */}
      {/* ... */}
    </div>
  );
}
```

#### 구현 계획

**1단계: Floating Action Button 추가** (20분)
```tsx
// page.tsx에 추가 (Line 169 이후)
{/* Mobile FAB - Show on screens < 1280px */}
<div className="xl:hidden fixed bottom-6 right-6 z-50">
  <button
    onClick={() => setBottomSheetOpen(true)}
    className="w-14 h-14 bg-gradient-to-r from-blue-500 to-blue-600
               rounded-full shadow-2xl flex items-center justify-center
               hover:scale-110 transition-transform duration-200"
  >
    <Target className="h-6 w-6 text-white" />
    {/* Notification badge */}
    {notificationCount > 0 && (
      <div className="absolute -top-1 -right-1 w-5 h-5 bg-red-500
                      rounded-full flex items-center justify-center">
        <span className="text-white text-xs font-bold">
          {notificationCount > 9 ? '9+' : notificationCount}
        </span>
      </div>
    )}
  </button>
</div>
```

**2단계: Bottom Sheet 구현** (30분)
```tsx
// Bottom Sheet Modal
{bottomSheetOpen && (
  <>
    {/* Backdrop */}
    <div
      className="fixed inset-0 bg-black/50 z-40 xl:hidden"
      onClick={() => setBottomSheetOpen(false)}
    />

    {/* Bottom Sheet */}
    <div className="fixed inset-x-0 bottom-0 z-50 bg-[#2a2a2a]
                    border-t border-gray-700 rounded-t-3xl shadow-2xl
                    max-h-[80vh] overflow-hidden xl:hidden
                    transition-transform duration-300">
      {/* Drag handle */}
      <div className="h-12 flex items-center justify-center">
        <div className="w-12 h-1.5 bg-gray-600 rounded-full" />
      </div>

      {/* Content */}
      <div className="p-4 overflow-y-auto max-h-[calc(80vh-3rem)]">
        <QuickActionPanel />
      </div>
    </div>
  </>
)}
```

**3단계: 상태 관리 추가** (10분)
```tsx
// page.tsx 상단에 추가
const [bottomSheetOpen, setBottomSheetOpen] = useState(false)
const [notificationCount, setNotificationCount] = useState(3) // 임시값
```

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 2.4 참조

### P2: Holdings/Watchlist 탭 통합 (🟠 높음)

⚠️ **중요**: Holdings와 Watchlist 모두 **실제 데이터 사용** (Mock 아님)
- **Holdings**: WebSocket 실시간 보유 종목 데이터 (변경 불필요)
- **Watchlist**: WebSocket + API (종목 추가/삭제 기능 필요)

**파일**:
- `stock-trading-ui/src/app/page.tsx` (Line 69-76)
- `stock-trading-ui/src/components/trading/HoldingsPanel.tsx`
- `stock-trading-ui/src/components/trading/WatchlistPanel.tsx`

#### 현황 분석
```tsx
// page.tsx: 각각 100% 너비로 분리
<div className="space-y-6">
  <HoldingsPanel />              // Row 2: 100% width (실제 데이터)
</div>

<div className="space-y-6">
  <WatchlistPanel />             // Row 3: 100% width (실제 데이터)
</div>
```
- ⚠️ 동일한 테이블 구조를 2번 렌더링
- ⚠️ 공간 낭비 (각각 풀 너비로 2개 행 차지)
- ⚠️ 스크롤 거리 증가 (4+ 화면)
- ✅ 데이터 소스: 모두 실제 WebSocket/API (Mock 아님)

#### 구현 계획

**1단계: AssetPanel 컴포넌트 생성** (30분)
```tsx
// components/trading/AssetPanel.tsx (신규)
export function AssetPanel() {
  const [activeTab, setActiveTab] = useState<'holdings' | 'watchlist' | 'alerts'>('holdings')

  return (
    <Card>
      {/* Tab Navigation - QuickActionPanel의 TabButton 재사용 */}
      <div className="flex border-b border-gray-700">
        <TabButton
          active={activeTab === 'holdings'}
          onClick={() => setActiveTab('holdings')}
          icon={Briefcase}
          label="보유 종목 (5)"
        />
        <TabButton
          active={activeTab === 'watchlist'}
          onClick={() => setActiveTab('watchlist')}
          icon={Eye}
          label="관심 종목 (12)"
        />
        <TabButton
          active={activeTab === 'alerts'}
          onClick={() => setActiveTab('alerts')}
          icon={Bell}
          label="알림 (3)"
        />
      </div>

      {/* Tab Content */}
      <CardContent>
        {activeTab === 'holdings' && <HoldingsTable />}
        {activeTab === 'watchlist' && <WatchlistTable />}
        {activeTab === 'alerts' && <AlertsPanel />}
      </CardContent>
    </Card>
  )
}
```

**2단계: 공통 테이블 컴포넌트 추출** (30분)
```tsx
// components/trading/AssetTable.tsx (신규)
interface AssetTableProps {
  data: (HoldingItem | WatchlistItem)[]
  type: 'holdings' | 'watchlist'
  onRowClick?: (code: string) => void
}
```

**3단계: page.tsx 업데이트** (15분)
```tsx
// page.tsx: 2개 패널을 1개로 통합
<div className="space-y-6">
  <AssetPanel />  // Replace both HoldingsPanel + WatchlistPanel
</div>

// 결과: Row 2 → Row 3으로 축소 (1개 행 절약)
```

**기대 효과**:
- 🎯 수직 공간 40% 절약 (1개 행 제거)
- 🎯 정보 접근성 향상 (탭 클릭만으로 전환)
- 🎯 코드 중복 제거 (공통 테이블 로직)

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 2.2 참조

---

### P3: 그리드 레이아웃 최적화 (🟠 높음)

**파일**: `stock-trading-ui/src/app/page.tsx` (Line 57-81)

#### 현황 분석
```tsx
// 현재: 4개 행으로 구성 (4+ 화면 스크롤 필요)
Row 1: [Portfolio Summary 50%] [Portfolio Performance 50%]
Row 2: [Holdings Panel 100%]
Row 3: [Watchlist Panel 100%]
Row 4: [Market Overview 100%]
```

#### 제안 레이아웃
```tsx
// 개선안: 3개 행으로 축소 (2 화면으로 축소)
Row 1: [Portfolio Summary 40%] [Portfolio Performance 60%]  // 차트 공간 확대
Row 2: [AssetPanel (Holdings/Watchlist Tabs) 50%] [Market Overview 50%]  // 동일 행 배치
Row 3: (선택사항) 향후 추가 위젯
```

#### 구현 계획

**1단계: Grid 시스템 변경** (30분)
```tsx
// page.tsx Line 57-81 교체
<div className="p-4 md:p-6 space-y-4 md:space-y-6 max-w-[1600px] mx-auto">
  {/* Row 1: Portfolio Cards (40:60 ratio for chart space) */}
  <div className="grid grid-cols-1 lg:grid-cols-10 gap-4 lg:gap-6">
    <div className="lg:col-span-4">
      <PortfolioSummary />
    </div>
    <div className="lg:col-span-6">
      <PortfolioPerformance className="h-full" />
    </div>
  </div>

  {/* Row 2: Assets + Market (50:50 for better balance) */}
  <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 lg:gap-6">
    <AssetPanel />         {/* Holdings + Watchlist tabs */}
    <MarketOverview />     {/* Moved up from bottom! */}
  </div>
</div>
```

**2단계: 반응형 테스트** (30분)
- Mobile (< 640px): 모두 100% 너비로 스택
- Tablet (640-1023px): 2-column 레이아웃
- Desktop (1024px+): 최적화된 그리드

**기대 효과**:
- 🎯 스크롤 40% 감소 (4개 행 → 2-3개 행)
- 🎯 Market Overview 가시성 향상 (하단 → 중간)
- 🎯 Performance Chart 공간 확대 (50% → 60%)

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 1.2 참조

### P4: 알림 시스템 구축 (🟡 중간)

**파일**: `stock-trading-ui/src/components/layout/Header.tsx`

#### 현황 분석
- ⚠️ Bell 아이콘은 있지만 기능 없음
- ⚠️ 실시간 알림 시스템 부재
- ⚠️ 사용자가 중요 이벤트를 놓칠 수 있음
- ⚠️ **Notification 타입 불일치**: 현재 types.ts는 'info' | 'success' | 'warning' | 'error'만 정의, 사용자 레벨 알림('order', 'alert', 'system') 부재

#### 타입 정의 수정

**파일**: `stock-trading-ui/src/lib/types.ts`

**현재 문제점**:
- 기존 Notification 타입은 시스템 내부 알림(토스트 메시지)만 지원
- 사용자 레벨 알림(매매 체결, 시스템 알림)과 분리 필요

**수정 계획**:
```typescript
// 사용자 레벨 알림 (Header에 표시)
export type UserNotificationType = 'order' | 'alert' | 'system';

export interface UserNotification {
  id: string;
  type: UserNotificationType;
  title: string;
  message: string;
  timestamp: Date;
  read: boolean;
  actionable?: boolean;
  action?: () => void;
  metadata?: {
    stockCode?: string;      // 관련 종목 코드
    orderType?: 'buy' | 'sell';  // 주문 유형
    amount?: number;          // 금액
  };
}

// 시스템 내부 알림 (Toast 메시지)
export type SystemNotificationType = 'info' | 'success' | 'warning' | 'error';

export interface SystemNotification {
  id: string;
  type: SystemNotificationType;
  message: string;
  duration?: number;
}
```

#### Header 개선 계획

**추가 메뉴 (향후 구현)**:
1. **매수/매도 메뉴**
   - `/trview?mode=buy`로 연결
   - `/trview?mode=sell`로 연결
   - 특정 종목 선택 및 차트 기반 매매 화면

2. **사용자 정보 메뉴**
   - 로그인 정보
   - 설정
   - Notification 관리

#### 구현 계획 (간단 버전)

**1단계: 알림 배지 추가** (30분)
```tsx
// Header.tsx에 추가
import { useLocalStorage } from '@/lib/hooks/useLocalStorage';
import { mockUserNotifications } from '@/lib/mock/userNotifications';

const [userNotifications, setUserNotifications] = useLocalStorage<UserNotification[]>(
  'user-notifications',
  mockUserNotifications
);

const unreadCount = userNotifications.filter(n => !n.read).length;

<Button variant="ghost" className="relative" onClick={() => setNotificationsOpen(true)}>
  <Bell className="h-4 w-4" />
  {unreadCount > 0 && (
    <div className="absolute -top-1 -right-1 w-5 h-5 bg-red-500 rounded-full
                    flex items-center justify-center text-white text-xs font-bold animate-pulse">
      {unreadCount > 9 ? '9+' : unreadCount}
    </div>
  )}
</Button>
```

**2단계: UserNotificationDropdown 컴포넌트** (1시간)
- `components/common/UserNotificationDropdown.tsx` 신규 생성
- 알림 목록 표시 (주문 체결, RSI 알림, 시스템 알림)
- "모두 읽음" 버튼
- 개별 알림 클릭 시 읽음 처리
- Mock 데이터: `lib/mock/userNotifications.ts` 사용

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 2.5 참조

---

### P5: MarketOverview 시각 계층 개선 (🟡 중간)

**파일**: `stock-trading-ui/src/components/trading/MarketOverview.tsx` (Line 90-154)

#### 현황 분석
```tsx
// Line 92-93: 섹션 헤더가 약함
<h4 className="text-sm font-bold text-white mb-4 uppercase tracking-wide">
  Major Indices
</h4>
```
- ✅ 기능적으로는 정상 작동
- ⚠️ 시각적 계층 구조가 약함 (인덱스 vs 주식 구분 불명확)
- ⚠️ Top Movers에 순위 표시 없음

#### 구현 계획 (간단 작업)

**1단계: 섹션 헤더 강화** (15분)
```tsx
// 기존 h4 태그에 스타일 추가
<h4 className="text-sm font-bold text-white mb-4 uppercase tracking-wide
               border-b-2 border-blue-500 pb-2">
  📈 Major Indices
</h4>
```

**2단계: 순위 배지 추가** (15분)
```tsx
// Top Movers에 순위 표시
<div className="flex items-center gap-2">
  <div className={cn(
    'w-6 h-6 rounded-full flex items-center justify-center text-xs font-bold',
    index < 3 ? 'bg-amber-500 text-white' : 'bg-gray-600 text-gray-300'
  )}>
    {index + 1}
  </div>
  {/* 주식 정보 */}
</div>
```

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 2.3 참조

### P6: 동적 그리드 레이아웃 (🟢 낮음 - 향후 계획)

**파일**: `stock-trading-ui/src/app/page.tsx` (전체 재구성)

#### 개념
- 사용자가 드래그 앤 드롭으로 카드 위치/크기 조절
- 레이아웃 프리셋 저장 (로컬 스토리지 또는 백엔드)
- 개인화된 대시보드 경험 제공

#### 구현 방향 (향후)
```bash
npm install react-grid-layout
```

**예상 소요**: 4-6시간
**우선순위**: 낮음 (P1-P5 완료 후 검토)

**📝 상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md` Section 5 참조

---

## 4. 리스크 분석 및 대응 전략

### 4.1 핵심 리스크 및 완화 조치

#### 🔴 **RISK-1: 알림 시스템 데이터 소스 불명확**

**문제점**:
- UI에 알림 배지 + 드롭다운만 구현되어 있고, 실제 알림 데이터 소스가 없음
- WebSocket 연동 방법 미정의
- 빈 알림 UI만 배포될 위험

**완화 조치**:
```tsx
// 1단계: Mock 데이터로 UI 구현 (Phase 1)
const mockNotifications: Notification[] = [
  { id: '1', type: 'order', title: 'Order Executed', ... },
  // ... 더미 데이터
]

// 2단계: 로컬 스토리지 기반 임시 구현 (Phase 2)
const [notifications, setNotifications] = useLocalStorage<Notification[]>(
  'notifications',
  mockNotifications
)

// 3단계: 실제 WebSocket 연동 (Phase 3 또는 별도 작업)
// Backend 작업 필요 (Section 4.2 참조)
useEffect(() => {
  const ws = new WebSocket(`${WS_URL}/notifications`)
  ws.onmessage = (event) => {
    const notification = JSON.parse(event.data)
    setNotifications(prev => [notification, ...prev])
  }
  return () => ws.close()
}, [])
```

**Backend 요구사항** (Section 4.2에서 상세):
- WebSocket 엔드포인트: `/ws/notifications`
- 알림 타입: `order`, `alert`, `system`
- Push 주기: 실시간 (주문 체결 즉시)

---

#### 🟠 **RISK-2: QuickActionPanel 재사용 설계 모호**

**문제점**:
- Desktop (sidebar)과 Mobile (bottom sheet)에서 동일 컴포넌트 재사용 계획
- Props 계약, 상태 관리, 열기/닫기 로직 불명확
- Focus trap, scroll locking 미정의 → 접근성 문제

**완화 조치**:

**컴포넌트 계약 정의**:
```tsx
// QuickActionPanel.tsx - 공통 인터페이스
interface QuickActionPanelProps {
  className?: string
  defaultTab?: 'watch' | 'trade' | 'alerts'
  onStockSelect?: (code: string) => void

  // 모바일 전용 props (optional)
  isSheet?: boolean           // Bottom sheet 모드인지 여부
  onClose?: () => void        // 닫기 핸들러 (sheet 모드일 때만)
}

// Desktop 사용
<QuickActionPanel className="hidden xl:flex" />

// Mobile Bottom Sheet 사용
<QuickActionPanel
  isSheet={true}
  onClose={() => setBottomSheetOpen(false)}
/>
```

**접근성 요구사항**:
```tsx
// Bottom Sheet 구현 시 필수 포함
import FocusTrap from 'focus-trap-react'

<FocusTrap active={bottomSheetOpen}>
  <div
    role="dialog"
    aria-modal="true"
    aria-labelledby="quick-action-title"
    onKeyDown={(e) => {
      if (e.key === 'Escape') setBottomSheetOpen(false)
    }}
  >
    {/* Content */}
  </div>
</FocusTrap>

// Scroll locking
useEffect(() => {
  if (bottomSheetOpen) {
    document.body.style.overflow = 'hidden'
  }
  return () => {
    document.body.style.overflow = 'unset'
  }
}, [bottomSheetOpen])
```

---

#### 🟠 **RISK-3: Holdings/Watchlist 통합 시 API 형태 차이**

**문제점**:
- `HoldingsPanel`과 `WatchlistPanel`이 다른 API 엔드포인트 사용 가능성
- 컬럼 구조, 정렬 기본값, 페이지네이션 규칙 차이
- 무분별한 리팩토링으로 범위 확대 위험

**완화 조치**:

**사전 조사 체크리스트** (구현 전 필수):
```bash
# 1. API 엔드포인트 확인
GET /api/account/holdings  # 보유 종목
GET /api/watchlist         # 관심 종목

# 2. 응답 구조 비교
# HoldingsPanel 데이터 구조
interface HoldingItem {
  stock_code: string
  stock_name: string
  quantity: number
  avg_price: number
  current_price: number
  profit_loss: number
  profit_rate: number
}

# WatchlistPanel 데이터 구조
interface WatchlistItem {
  stock_code: string
  stock_name: string
  current_price: number
  change_rate: number
  rsi: number
  macd: number
}

# 3. 공통 인터페이스 정의
type AssetItem = HoldingItem | WatchlistItem

function isHolding(item: AssetItem): item is HoldingItem {
  return 'quantity' in item
}
```

**단계별 안전 전략**:
```tsx
// Phase 1: UI만 통합 (데이터 fetching은 기존 유지)
export function AssetPanel() {
  // ⚠️ 실제 WebSocket 데이터 사용 (Mock 아님)
  const { holdings } = useAccountData()      // 기존 훅 유지 (실제 데이터)
  const { watchlist } = useWatchlistData()   // 기존 훅 유지 (실제 데이터)

  // 탭만 새로 구현
  return (
    <Card>
      <TabNavigation />
      {activeTab === 'holdings' && <HoldingsTable data={holdings} />}  {/* 실제 데이터 */}
      {activeTab === 'watchlist' && <WatchlistTable data={watchlist} />}  {/* 실제 데이터 */}
    </Card>
  )
}

// Phase 2: 테이블 로직 통합 (별도 PR)
// Phase 3: Watchlist 종목 추가/삭제 기능 구현
```

---

#### 🟡 **RISK-4: PortfolioPerformance 차트 번들 크기 및 데이터 가용성**

**문제점**:
- Recharts 라이브러리 번들 크기 약 **100KB** (gzipped: ~30KB)
- Tree-shaking, dynamic import 미고려
- 과거 데이터 API 가용성 미확인 → 1시간 예상이 낙관적

**완화 조치**:

**번들 최적화**:
```tsx
// 1. Dynamic import (코드 스플리팅)
import dynamic from 'next/dynamic'

const PerformanceChart = dynamic(
  () => import('@/components/trading/PerformanceChart'),
  {
    loading: () => <ChartSkeleton />,
    ssr: false  // 클라이언트에서만 로드
  }
)

// 2. Tree-shaking (필요한 컴포넌트만 import)
import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'
// ❌ import * as Recharts from 'recharts'  // 전체 import 금지
```

**데이터 가용성 확인 체크리스트**:
```bash
# 구현 전 필수 확인
1. [ ] 백엔드 API 엔드포인트 존재 확인
   GET /api/portfolio/history?period=1M

2. [ ] 응답 데이터 형태 확인
   {
     "data": [
       { "date": "2025-10-01", "value": 10000000, "kospi": 2500 },
       ...
     ]
   }

3. [ ] 타임존 처리 방식 확인
   - UTC vs KST
   - 장 시작/종료 시간 고려

4. [ ] 데이터 없을 경우 대응
   - Mock 데이터 사용
   - Empty State 표시
```

**현실적인 예상 시간**:
- API 있음: 3-4시간 (계획대로)
- API 없음 (Mock): 2-3시간
- API 개발 필요: **백엔드 작업 별도 (6-8시간)** + 프론트엔드 3-4시간

---

#### 🟡 **RISK-5: 10-column Grid 시스템 Tailwind 미지원**

**문제점**:
- Tailwind 기본 grid는 `grid-cols-12`까지만 지원
- `grid-cols-10`, `col-span-4`, `col-span-6` 등이 기본 제공 안 됨
- CSS 클래스가 실제로 작동하지 않을 위험

**완화 조치**:

**Tailwind 설정 확장 필요**:
```js
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      gridTemplateColumns: {
        '10': 'repeat(10, minmax(0, 1fr))',
      },
      gridColumn: {
        'span-4': 'span 4 / span 4',
        'span-6': 'span 6 / span 6',
      }
    }
  }
}
```

**대안: 기존 12-column 활용**:
```tsx
// 권장: Tailwind 기본 12-column 사용
<div className="grid grid-cols-1 lg:grid-cols-12 gap-4 lg:gap-6">
  <div className="lg:col-span-5">  {/* 5/12 ≈ 40% */}
    <PortfolioSummary />
  </div>
  <div className="lg:col-span-7">  {/* 7/12 ≈ 60% */}
    <PortfolioPerformance />
  </div>
</div>

// 50:50 분할
<div className="grid grid-cols-1 lg:grid-cols-2 gap-4 lg:gap-6">
  <AssetPanel />
  <MarketOverview />
</div>
```

---

## 5. Backend/API 작업 정의

### 5.1 필수 Backend 작업 (프론트엔드와 병렬 진행)

⚠️ **주의**: Holdings와 Portfolio Summary는 이미 실제 WebSocket 연동 완료 (추가 작업 불필요)

| API 엔드포인트 | 메서드 | 목적 | 우선순위 | 예상 소요 |
|--------------|--------|------|---------|----------|
| `/api/portfolio/history` | GET | 포트폴리오 차트 시계열 데이터 | 🔴 높음 | 4-6시간 |
| `/api/watchlist` | GET | 관심종목 목록 조회 | 🔴 높음 | 2시간 |
| `/api/watchlist` | POST | 관심종목 추가 | 🔴 높음 | 2시간 |
| `/api/watchlist/:code` | DELETE | 관심종목 삭제 | 🔴 높음 | 1시간 |
| `/ws/notifications` | WebSocket | 실시간 알림 Push | 🟠 중간 | 3-4시간 |
| `/api/notifications` | GET | 알림 목록 조회 | 🟠 중간 | 2시간 |
| `/api/notifications/:id/read` | PATCH | 알림 읽음 처리 | 🟡 낮음 | 1시간 |

### 5.2 Mock Data Layer (임시 대응)

**⚠️ 중요: Mock 데이터 사용 범위 명확화**

**Mock 데이터를 사용하는 부분 (Phase 구현 시):**
1. **UserNotification**: 알림 시스템 (localStorage 기반)
2. **PortfolioHistory (차트)**: 포트폴리오 성과 차트 (localStorage 기반)

**실제 데이터를 사용하는 부분 (변경 불필요):**
1. **Holdings (보유 종목)**: WebSocket 실시간 데이터 (이미 구현됨)
2. **Watchlist (관심종목)**: WebSocket + API (종목 추가/삭제 기능)
3. **Portfolio Summary (포트폴리오 요약)**: WebSocket 실시간 자산/수익률

**프론트엔드에서 즉시 사용 가능한 Mock 데이터**:
```tsx
// lib/mock/portfolioHistory.ts (차트 히스토리 전용)
export const mockPortfolioHistory = {
  '1D': generateDailyData(24),    // 24시간 데이터
  '1W': generateDailyData(7),     // 7일 데이터
  '1M': generateDailyData(30),    // 30일 데이터
  '3M': generateDailyData(90),
  '1Y': generateDailyData(365),
}

// lib/mock/userNotifications.ts (알림 전용)
export const mockUserNotifications: UserNotification[] = [
  {
    id: '1',
    type: 'order',
    title: '매수 체결 완료',
    message: '삼성전자 100주가 73,000원에 체결되었습니다.',
    timestamp: new Date(Date.now() - 5 * 60 * 1000),
    read: false,
    metadata: {
      stockCode: '005930',
      orderType: 'buy',
      amount: 7300000
    }
  },
  // ... 더미 데이터
]
```

**TODO 플래그 명확화**:
```tsx
// PortfolioPerformance.tsx - 차트 영역만 Mock 사용
const { summary } = useRealtimeAccountData()  // Summary는 실제 데이터
const { chartData, isLoading } = usePortfolioHistory(timePeriod)

// TODO: Replace with real API when /api/portfolio/history is ready
const historicalData = chartData || mockPortfolioHistory[timePeriod]
```

---

### 5.3 Mock 파일 구조 상세

**파일 1**: `lib/mock/userNotifications.ts`
```typescript
import { UserNotification } from '@/lib/types';

export const mockUserNotifications: UserNotification[] = [
  {
    id: '1',
    type: 'order',
    title: '매수 체결 완료',
    message: '삼성전자 100주가 73,000원에 체결되었습니다.',
    timestamp: new Date(Date.now() - 5 * 60 * 1000),
    read: false,
    metadata: {
      stockCode: '005930',
      orderType: 'buy',
      amount: 7300000
    }
  },
  {
    id: '2',
    type: 'alert',
    title: 'RSI 과매수 신호',
    message: 'SK하이닉스의 RSI가 70을 초과했습니다.',
    timestamp: new Date(Date.now() - 10 * 60 * 1000),
    read: false,
    metadata: {
      stockCode: '000660'
    }
  },
  {
    id: '3',
    type: 'system',
    title: '시장 마감',
    message: 'KOSPI 시장이 마감되었습니다.',
    timestamp: new Date(Date.now() - 2 * 60 * 60 * 1000),
    read: true,
  },
];
```

**파일 2**: `lib/hooks/useLocalStorage.ts`
```typescript
import { useState, useEffect } from 'react';

export function useLocalStorage<T>(key: string, initialValue: T) {
  const [storedValue, setStoredValue] = useState<T>(() => {
    if (typeof window === 'undefined') {
      return initialValue;
    }
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      console.warn(`Error loading localStorage key "${key}":`, error);
      return initialValue;
    }
  });

  const setValue = (value: T | ((val: T) => T)) => {
    try {
      const valueToStore = value instanceof Function ? value(storedValue) : value;
      setStoredValue(valueToStore);
      if (typeof window !== 'undefined') {
        window.localStorage.setItem(key, JSON.stringify(valueToStore));
      }
    } catch (error) {
      console.warn(`Error saving localStorage key "${key}":`, error);
    }
  };

  return [storedValue, setValue] as const;
}
```

---

## 6. 테스트 요구사항 및 Acceptance Criteria

### 6.1 컴포넌트 테스트

**필수 테스트 커버리지**:
```tsx
// AssetPanel.test.tsx
describe('AssetPanel', () => {
  it('should switch tabs correctly', () => {
    render(<AssetPanel />)
    fireEvent.click(screen.getByText('관심 종목'))
    expect(screen.getByText('WatchlistTable content')).toBeInTheDocument()
  })

  it('should show empty state when no holdings', () => {
    render(<AssetPanel />, { holdings: [] })
    expect(screen.getByText('No Holdings Yet')).toBeInTheDocument()
  })
})

// BottomSheet.test.tsx
describe('Bottom Sheet', () => {
  it('should trap focus when open', () => {
    render(<BottomSheet open={true} />)
    // Focus trap test
  })

  it('should close on Escape key', () => {
    const onClose = jest.fn()
    render(<BottomSheet open={true} onClose={onClose} />)
    fireEvent.keyDown(document, { key: 'Escape' })
    expect(onClose).toHaveBeenCalled()
  })
})
```

### 6.2 모바일 FAB Acceptance Criteria

**기능 요구사항**:
- [ ] FAB는 1280px 미만에서만 표시 (`xl:hidden`)
- [ ] FAB 클릭 시 Bottom Sheet 애니메이션 (300ms)
- [ ] Backdrop 클릭 시 Sheet 닫힘
- [ ] ESC 키로 Sheet 닫힘
- [ ] Sheet 열릴 때 body scroll 잠김
- [ ] Sheet 닫힐 때 body scroll 복원
- [ ] 알림 배지 (3개 이상일 때 "3+" 표시)
- [ ] Focus trap 작동 (Tab 키로 Sheet 내부만 이동)

**접근성 요구사항**:
- [ ] `role="dialog"` 속성
- [ ] `aria-modal="true"` 속성
- [ ] `aria-labelledby` 속성 (Sheet 제목)
- [ ] Safe area padding (iOS notch 고려)
- [ ] 44x44px 최소 터치 영역 (Apple HIG)

**제스처 지원** (선택사항, Phase 2):
- [ ] Drag handle로 Sheet 닫기
- [ ] Swipe down으로 Sheet 닫기

### 6.3 시각적 회귀 테스트

**권장 도구**: Playwright + Percy/Chromatic

```bash
# Visual regression tests
npx playwright test --update-snapshots  # Baseline 생성
npx playwright test                      # 비교 테스트
```

**핵심 스냅샷**:
- [ ] Dashboard - Desktop (1920x1080)
- [ ] Dashboard - Tablet (768x1024)
- [ ] Dashboard - Mobile (375x812)
- [ ] Bottom Sheet - Open state
- [ ] Performance Chart - All time periods
- [ ] AssetPanel - All tabs

---

## 7. 배포 전략 (Feature Flags & Rollback)

### 7.1 Feature Flag 구조

**high-impact 변경사항은 Feature Flag 뒤에 배포**:

```tsx
// lib/featureFlags.ts
export const FEATURE_FLAGS = {
  NEW_GRID_LAYOUT: process.env.NEXT_PUBLIC_FF_NEW_GRID === 'true',
  ASSET_PANEL_TABS: process.env.NEXT_PUBLIC_FF_ASSET_PANEL === 'true',
  RECHARTS_ENABLED: process.env.NEXT_PUBLIC_FF_RECHARTS === 'true',
  MOBILE_FAB: process.env.NEXT_PUBLIC_FF_MOBILE_FAB === 'true',
} as const

// page.tsx
import { FEATURE_FLAGS } from '@/lib/featureFlags'

export default function Home() {
  return (
    <div>
      {FEATURE_FLAGS.NEW_GRID_LAYOUT ? (
        <NewGridLayout />
      ) : (
        <LegacyLayout />
      )}
    </div>
  )
}
```

**환경변수 설정**:
```bash
# .env.local (로컬 테스트)
NEXT_PUBLIC_FF_NEW_GRID=true
NEXT_PUBLIC_FF_ASSET_PANEL=true
NEXT_PUBLIC_FF_RECHARTS=false  # 아직 API 없음
NEXT_PUBLIC_FF_MOBILE_FAB=true

# Production (점진적 롤아웃)
# 1주차: 10% 사용자
# 2주차: 50% 사용자
# 3주차: 100% 사용자
```

### 7.2 Rollback 계획

**긴급 Rollback 시나리오**:

| 변경사항 | Rollback 방법 | 예상 시간 | 데이터 손실 |
|---------|--------------|----------|-----------|
| Grid Layout | Feature Flag OFF | 즉시 | 없음 |
| AssetPanel Tabs | Feature Flag OFF | 즉시 | 없음 |
| Recharts | Dynamic import 제거 | 5분 | 없음 |
| Mobile FAB | CSS `display: none` | 즉시 | 없음 |

**Rollback 트리거**:
- 에러율 5% 이상 증가
- Lighthouse 성능 점수 10점 이상 하락
- 사용자 이탈률 20% 이상 증가
- 치명적 접근성 이슈 발견

**Rollback 프로세스**:
```bash
# 1. Feature Flag 비활성화
# Vercel/환경변수 설정에서 즉시 변경
NEXT_PUBLIC_FF_NEW_GRID=false

# 2. 즉시 재배포 (Vercel은 30초 내)
vercel --prod

# 3. 모니터링 (5분간)
# - Sentry 에러율 확인
# - Google Analytics 이탈률 확인

# 4. 코드 Revert (Feature Flag가 실패할 경우)
git revert <commit-hash>
git push origin main
```

---

## 8. 실행 체크리스트

### ⚠️ 구현 전 필수 확인 사항
- [ ] **RISK-3**: API 형태 확인
  - Holdings API 응답 구조 확인
  - Watchlist API 응답 구조 확인
  - 공통 인터페이스 정의
- [ ] **RISK-4**: 포트폴리오 히스토리 API 확인
  - `/api/portfolio/history` 엔드포인트 존재 여부
  - 없을 경우: Mock 데이터 사용 결정
- [ ] **RISK-5**: Tailwind Grid 확인
  - 10-column grid 필요 시 `tailwind.config.js` 확장
  - 또는 12-column grid 사용 결정 (권장)
- [ ] **Feature Flag 설정**
  - `.env.local` 파일 생성
  - Feature flag 환경변수 정의

---

### Phase 0: 사전 준비 및 환경 설정 (30분-1시간)

**브랜치 확인**:
- [ ] 현재 브랜치: `feature/trade` 확인
- [ ] Git 상태 확인 (`git status`)

**의존성 설치**:
- [ ] `focus-trap-react` 설치
- [ ] `recharts` 설치
- [ ] package.json 업데이트 확인
- [ ] `npm install` 실행 및 성공 확인

**Mock 인프라 구축**:
- [ ] `lib/mock` 디렉터리 생성
- [ ] `lib/hooks` 디렉터리 생성
- [ ] `lib/hooks/useLocalStorage.ts` 생성
  - [ ] useState, useEffect 구현
  - [ ] localStorage 읽기/쓰기 로직
  - [ ] SSR 대응 (window undefined 체크)
  - [ ] 제네릭 타입 지원
- [ ] `lib/mock/userNotifications.ts` 생성
  - [ ] Mock UserNotification 데이터 3-5개
  - [ ] 다양한 타입 포함 (order, alert, system)
  - [ ] metadata 필드 포함
- [ ] `lib/mock/portfolioHistory.ts` 생성 (Phase 2용)
  - [ ] 기본 구조만 정의
  - [ ] generateDailyData 헬퍼 함수 작성 (향후)

**타입 정의 수정**:
- [ ] `lib/types.ts` 열기
- [ ] UserNotificationType 타입 추가 ('order' | 'alert' | 'system')
- [ ] UserNotification 인터페이스 추가
  - [ ] metadata 필드 포함
- [ ] SystemNotificationType 타입 추가 ('info' | 'success' | 'warning' | 'error')
- [ ] SystemNotification 인터페이스 추가
- [ ] 기존 Notification 타입 검토
  - [ ] SystemNotification으로 변경할지 결정
  - [ ] 또는 별도 유지 (하위 호환성)

**검증**:
- [ ] TypeScript 컴파일 에러 없음
- [ ] 의존성 설치 완료 확인
- [ ] Mock 파일 import 테스트
  - [ ] `import { mockUserNotifications } from '@/lib/mock/userNotifications'`
  - [ ] `import { useLocalStorage } from '@/lib/hooks/useLocalStorage'`
- [ ] 브라우저에서 기본 빌드 확인 (`npm run dev`)

---

### Phase 1: Quick Wins (4-5시간)

**사전 작업**:
- [ ] Mock 데이터 레이어 생성
  - `lib/mock/notifications.ts` 생성
  - `lib/mock/portfolioHistory.ts` 생성 (Phase 2용)

**UI 구현**:
- [ ] **P1-B**: 모바일 FAB + Bottom Sheet 추가 (1시간)
  - [ ] `page.tsx`에 FAB 버튼 추가
  - [ ] Bottom Sheet 모달 구현
  - [ ] Focus trap 추가 (`focus-trap-react` 설치 필요)
  - [ ] Scroll locking 구현
  - [ ] 상태 관리 추가
  - [ ] **Acceptance Criteria 확인** (Section 6.2)

- [ ] **P5**: MarketOverview 시각 계층 개선 (30분)
  - [ ] 섹션 헤더에 border-bottom 추가
  - [ ] Top Movers에 순위 배지 추가
  - [ ] 이모지 아이콘 추가

- [ ] **P4**: 알림 시스템 (Mock 데이터) (1-2시간)
  - [ ] Header에 알림 카운트 배지 추가
  - [ ] 드롭다운 컴포넌트 구현
  - [ ] Mock 알림 데이터 연동
  - [ ] TODO 주석 추가: "// TODO: Replace with WebSocket"

- [ ] **기타**: 에러/Empty State 컴포넌트 (30분)
  - [ ] `components/common/ErrorState.tsx` 생성
  - [ ] `components/common/EmptyState.tsx` 생성
  - [ ] 모든 패널에 적용

**테스트**:
- [ ] Bottom Sheet 컴포넌트 테스트 작성
- [ ] 모바일 디바이스 실제 테스트 (iOS Safari, Chrome Android)

### Phase 2: Core Features (6-8시간)

**P1-A: PortfolioPerformance 차트 (3-4시간)**:
- [ ] **사전 확인**
  - [ ] Backend API 가용성 확인 (RISK-4 참조)
  - [ ] API 없을 경우: Mock 데이터 사용 결정

- [ ] **번들 최적화 설정**
  - [ ] Dynamic import 구현
  - [ ] Tree-shaking 설정 확인
  - [ ] `next.config.js`에 번들 분석 추가

- [ ] **차트 구현**
  - [ ] Recharts 설치: `npm install recharts`
  - [ ] `components/trading/PerformanceChart.tsx` 생성
  - [ ] LineChart 컴포넌트 작성 (Portfolio + KOSPI)
  - [ ] 시간 기간 탭 추가 (TabButton 재사용)
  - [ ] KPI 카드 3개 추가 (Return, MDD, Volatility)
  - [ ] Mock 데이터 연동 또는 실제 API 연동
  - [ ] TODO 주석 추가: API 미구현 시

- [ ] **테스트**
  - [ ] 차트 렌더링 테스트
  - [ ] 시간 기간 전환 테스트
  - [ ] 번들 크기 확인 (<50KB 증가)

**P2: AssetPanel 탭 통합 (3-4시간)**:
- [ ] **사전 확인**
  - [ ] RISK-3 체크리스트 완료
  - [ ] API 형태 차이 분석 완료

- [ ] **단계별 안전 구현**
  - [ ] Phase 1: `components/trading/AssetPanel.tsx` 생성
  - [ ] 탭 네비게이션 구현 (TabButton 재사용)
  - [ ] 기존 `HoldingsTable`, `WatchlistTable` 재사용
  - [ ] 기존 훅 유지 (`useAccountData`, `useWatchlistData`)
  - [ ] `page.tsx` 업데이트 (2개 패널 → 1개로)

- [ ] **테스트**
  - [ ] 탭 전환 테스트
  - [ ] 데이터 로딩 테스트
  - [ ] Empty State 테스트

- [ ] **Feature Flag 설정**
  - [ ] `NEXT_PUBLIC_FF_ASSET_PANEL=true` 추가
  - [ ] Legacy 레이아웃 fallback 구현

### Phase 3: Layout Optimization (2-3시간)

**P3: 그리드 레이아웃 변경 (1시간)**:
- [ ] **Tailwind 설정 확인**
  - [ ] RISK-5: 12-column grid 사용 결정 (권장)
  - [ ] 또는 `tailwind.config.js` 확장

- [ ] **그리드 구조 변경**
  - [ ] `page.tsx` Line 57-81 업데이트
  - [ ] Row 1: 5/12 (Portfolio) + 7/12 (Performance) 비율
  - [ ] Row 2: AssetPanel + MarketOverview (50:50)
  - [ ] Row 3 제거 (Watchlist 통합 완료)

- [ ] **Feature Flag 설정**
  - [ ] `NEXT_PUBLIC_FF_NEW_GRID=true`
  - [ ] Legacy 레이아웃 보존

**반응형 테스트 (30분)**:
- [ ] 브레이크포인트별 테스트
  - [ ] Mobile (< 640px): 전체 스택
  - [ ] Tablet (640-1023px): 2-column
  - [ ] Desktop (1024px+): 최적화 그리드
- [ ] 브라우저 호환성
  - [ ] Chrome
  - [ ] Safari
  - [ ] Firefox
  - [ ] Mobile Safari (iOS)
  - [ ] Chrome Android

**Skeleton Loading (30분-1시간)**:
- [ ] 컴포넌트별 스켈레톤 생성
  - [ ] `PortfolioPerformanceSkeleton.tsx`
  - [ ] `AssetPanelSkeleton.tsx`
  - [ ] `MarketOverviewSkeleton.tsx`
- [ ] 로딩 상태 적용
- [ ] 애니메이션 추가 (pulse effect)

**시각적 회귀 테스트**:
- [ ] Playwright 스냅샷 업데이트
- [ ] 3가지 디바이스 크기 스냅샷
- [ ] Before/After 비교

---

### Phase 4: 품질 보증 및 배포 (2-3시간)

**테스트 완료**:
- [ ] 유닛 테스트 실행 및 커버리지 확인
- [ ] E2E 테스트 (Playwright)
- [ ] 접근성 테스트 (Axe DevTools)
- [ ] 성능 테스트 (Lighthouse)
  - [ ] Desktop 점수 > 90
  - [ ] Mobile 점수 > 80

**코드 리뷰 체크리스트**:
- [ ] TypeScript 에러 없음
- [ ] ESLint 경고 없음
- [ ] TODO 주석 명확히 표시
- [ ] Feature Flag 올바르게 구현
- [ ] Rollback 전략 검증

**배포 준비**:
- [ ] Feature Flag 환경변수 설정
  - [ ] Development: 모두 ON
  - [ ] Staging: 모두 ON
  - [ ] Production: 단계별 롤아웃
- [ ] 모니터링 설정
  - [ ] Sentry 에러 추적
  - [ ] Google Analytics 이벤트
  - [ ] Performance metrics
- [ ] Rollback 절차 문서화

**점진적 롤아웃 계획**:
- [ ] 1주차: 10% 사용자 (Feature Flag)
- [ ] 2주차: 50% 사용자
- [ ] 3주차: 100% 사용자
- [ ] 각 단계마다 모니터링 (Section 7.2 참조)

---

## 5. 기대 효과 및 성공 지표

### 사용자 경험 개선
| 지표 | 현재 | 목표 | 측정 방법 |
|-----|------|------|---------|
| 스크롤 거리 | 4+ 화면 | 2 화면 | 픽셀 수 측정 |
| 정보 접근 시간 | 8-10초 | 3-5초 | 사용자 테스트 |
| 모바일 사용성 (SUS 점수) | 40% | 85%+ | 설문 조사 |
| 차트 기능 만족도 | 0% (미구현) | 90%+ | 사용자 피드백 |

### 기술적 개선
| 지표 | 현재 | 목표 | 도구 |
|-----|------|------|------|
| First Contentful Paint | ~1.2s | <1.0s | Lighthouse |
| Time to Interactive | ~2.5s | <2.0s | Lighthouse |
| 번들 크기 증가 | - | <50KB | webpack-bundle-analyzer |
| 코드 중복 | 높음 (Holdings/Watchlist) | 낮음 (공통 테이블) | 코드 분석 |

### 비즈니스 영향
- **사용자 참여도**: +25% 예상 (세션 시간 증가)
- **모바일 사용자**: +60% 예상 (FAB/Bottom Sheet 도입)
- **기능 발견율**: +40% 예상 (알림 시스템)
- **지원 티켓**: -30% 예상 (명확한 에러/Empty State)

---

## 6. 참고 문서

### 상세 구현 가이드
📄 **`docs/plan/1010.dashboard.ux-design-detailed.md`**
- 70+ 페이지 상세 디자인 명세
- 섹션별 구현 코드 예시
- CSS 클래스 복사 가능
- 반응형 디자인 사양
- 접근성 가이드라인
- Before/After 비교

### 컴포넌트 위치 Quick Reference
```
stock-trading-ui/src/
├── app/
│   └── page.tsx                          # 메인 대시보드 (Line 57-81 수정 필요)
├── components/
│   ├── layout/
│   │   ├── Header.tsx                    # 알림 배지 추가 필요
│   │   └── QuickActionPanel.tsx          # Bottom Sheet로 재사용
│   └── trading/
│       ├── PortfolioPerformance.tsx      # 차트 구현 필요 (Line 37-46)
│       ├── HoldingsPanel.tsx             # AssetPanel로 통합 예정
│       ├── WatchlistPanel.tsx            # AssetPanel로 통합 예정
│       ├── MarketOverview.tsx            # 시각 계층 개선 (Line 92-93)
│       └── AssetPanel.tsx                # 신규 생성 필요
```

---

## 📌 요약

### 핵심 변경 사항
1. **PortfolioPerformance 차트 구현** → 데이터 시각화 완성
2. **모바일 FAB + Bottom Sheet** → 모바일 사용성 40% 이상 개선
3. **Holdings/Watchlist 탭 통합** → 공간 효율 40% 개선, 코드 중복 제거
4. **그리드 레이아웃 최적화** → 스크롤 40% 감소 (4개 행 → 2-3개 행)
5. **알림 시스템 구축** → 실시간 피드백 제공

### 다음 단계
1. **Phase 1 시작** (Quick Wins, 4-5시간)
2. **Phase 2 진행** (Core Features, 6-8시간)
3. **Phase 3 완료** (Layout Optimization, 2-3시간)
4. **전체 테스트 및 QA**
5. **사용자 피드백 수집**

**예상 총 소요 시간**:
- Frontend: 12-16시간 (2-3일 집중 작업)
- Backend (병렬): 10-13시간 (알림 WebSocket + 포트폴리오 히스토리 API)
- 테스트 & QA: 2-3시간
- **총계**: 24-32시간 (4-5일, Frontend+Backend 병렬 진행 시 3일 가능)

**예상 ROI**:
- 사용자 만족도 +50%
- 모바일 사용자 +60%
- 정보 접근 시간 -50%
- 지원 티켓 -30%
- 페이지 이탈률 -20%

---

## 부록 A: 주요 변경 사항 요약

| 섹션 | 내용 | 추가 이유 |
|-----|------|---------|
| **Section 4** | 리스크 분석 및 완화 조치 | 실제 구현 시 발생 가능한 5가지 핵심 리스크 대응 |
| **Section 5** | Backend/API 작업 정의 | Frontend와 병렬 진행할 백엔드 작업 명확화 |
| **Section 6** | 테스트 요구사항 & Acceptance Criteria | 품질 보증 및 측정 가능한 목표 설정 |
| **Section 7** | Feature Flag & Rollback 전략 | 안전한 배포 및 긴급 복구 계획 |
| **Section 8** | 상세 실행 체크리스트 | 단계별 구체적 작업 항목 및 검증 절차 |

## 부록 B: 긴급 연락 및 의사결정 프로세스

**구현 중 차단 발생 시**:
1. **API 미구현**: Mock 데이터 사용 → TODO 주석 → 백엔드 팀에 티켓 생성
2. **접근성 이슈**: 해당 기능 Feature Flag OFF → 수정 후 재배포
3. **성능 저하**: 번들 분석 → Dynamic import 추가 → 최적화
4. **TypeScript 에러**: 타입 확장 또는 `any` 임시 사용 → TODO 주석
5. **Rollback 필요**: Section 7.2 절차 따르기

**의사결정 권한**:
- **Frontend 단독 결정 가능**: UI/UX, 컴포넌트 구조, 스타일링
- **Backend 협의 필요**: API 스펙, 데이터 구조, WebSocket 프로토콜
- **PM 협의 필요**: Feature Flag 롤아웃 비율, Rollback 트리거
- **보안팀 협의 필요**: 사용자 데이터 처리, 알림 시스템 보안

---

## 부록 C: 참고 자료

### 문서
- **상세 구현 가이드**: `docs/plan/1010.dashboard.ux-design-detailed.md`
- **Backend API 스펙**: (생성 필요)
- **테스트 전략**: (생성 필요)

### 라이브러리 문서
- [Recharts](https://recharts.org/) - 차트 라이브러리
- [focus-trap-react](https://github.com/focus-trap/focus-trap-react) - Focus trap
- [Tailwind Grid](https://tailwindcss.com/docs/grid-template-columns) - Grid 시스템
- [Next.js Dynamic Import](https://nextjs.org/docs/advanced-features/dynamic-import) - 코드 스플리팅

### 접근성 가이드라인
- [WCAG 2.1 Level AA](https://www.w3.org/WAI/WCAG21/quickref/)
- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [Material Design Touch Targets](https://material.io/design/usability/accessibility.html)

---

**문서 작성**: Claude Code with Serena MCP + Trading System Designer
**리스크 분석 반영**: 2025-10-10 (v2.1)
**사용자 피드백 반영**: 2025-10-10 (v2.3)
**실제 데이터 사용 명확화**: 2025-10-10 (v2.4)
**최종 업데이트**: 2025-10-10
**버전**: 2.4

## 📋 변경 이력

### v2.4 (2025-10-10)
**변경 내용**: 실제 데이터 vs Mock 데이터 사용 명확화
- Holdings: 실제 WebSocket 데이터 사용 (Mock 사용 안 함)
- Watchlist: 실제 WebSocket + API 데이터 (종목 추가/삭제 기능)
- Portfolio Summary: 실제 WebSocket 데이터 (자산, 수익률)
- Portfolio Chart: Mock 또는 API 데이터 (차트 히스토리)
- UserNotification: Mock 데이터 (localStorage)
- Section 5.1: Watchlist API 추가 (GET, POST, DELETE)
- Section 5.2: Mock 데이터 사용 범위 명확화
- Phase 2 (P1-A): Portfolio Summary vs Chart 구분
- Phase 3 (P2): Holdings/Watchlist 실제 데이터 사용 명시

### v2.3 (2025-10-10)
**변경 내용**: Phase 0 추가, Notification 타입 분리, QuickActionPanel isSheet 지원

### v2.1 (2025-10-10)
**변경 내용**: 리스크 분석 반영

### v1.0 (초기)
**변경 내용**: 초기 계획 수립
