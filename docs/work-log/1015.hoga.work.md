# 2025-10-15 호가 데이터 실시간 연동 작업 로그

## 📋 작업 개요

**목표**: 프론트엔드에서 실시간 호가 데이터(OrderBook)가 0으로 표시되는 문제 해결
**시작 시간**: 2025-10-15 09:48 (KST)
**현재 상태**: 백엔드 WebSocket 연결 및 데이터 수신 성공, 프론트엔드 연결 확인 필요

## 🔧 시스템 환경

### 실행 중인 서비스들
- **백엔드 서버**: `uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload` (PID: 29435)
- **프론트엔드 서버**: `next dev --port 9000` (PID: 46097, 46131)
- **WebSocket 프로세스**: `python -m app.domestic_websocket` (PID: 32914)
- **Chrome 브라우저**: 디버깅 모드로 실행 중 (포트 9222)

### 포트 사용 현황
- **8000**: FastAPI 백엔드 서버
- **9000**: Next.js 프론트엔드 서버  
- **9222**: Chrome DevTools 디버깅 포트

## 📊 문제 진단 과정

### 1. 초기 문제 상황
- **증상**: 프론트엔드 호가창에서 모든 데이터가 0으로 표시
- **백엔드 API 테스트 결과**: 
  ```bash
  curl "http://localhost:8000/api/realtime/orderbook/005930"
  # 결과: current_price: 0, asks/bids 모두 0
  ```

### 2. WebSocket 초기화 문제 해결

#### A. WebSocket 프로세스 실행 문제
- **문제**: `domestic_websocket.py` 단독 실행 시 초기화 오류
- **해결 과정**:
  1. `KoreaInvestAPIService.__init__() missing 1 required positional argument: 'settings'` 오류
  2. `ModuleNotFoundError: No module named 'app.core.settings'` 오류
  3. `TypeError: KoreaInvestEnv.__init__() missing 1 required positional argument: 'config'` 오류
  4. `AttributeError: 'KoreaInvestAPIService' object has no attribute 'stock_account_number'` 오류

#### B. WebSocket 초기화 코드 수정
**파일**: `/home/wide/projects/systrading/backend/app/domestic_websocket.py`

```python
if __name__ == "__main__":
    import multiprocessing
    import sys
    import os
    
    # 프로젝트 루트를 Python 경로에 추가
    sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
    
    from app.core.korea_invest import KoreaInvestAPIService
    from app.core.logging_config import setup_logging
    from app.core.config import get_settings
    from loguru import logger
    
    # 로깅 설정
    setup_logging()
    logger.info("=== domestic_websocket 단독 실행 시작 ===")
    
    # Settings 로드 (config.yaml + 토큰 관리)
    settings = get_settings()
    logger.info(f"Settings 로드 완료: Using URL = {settings.KI_USING_URL}")
    
    # KoreaInvestAPIService 초기화 (main.py와 동일한 방식)
    korea_invest_service = KoreaInvestAPIService(settings)
    
    if not korea_invest_service.api_instance:
        logger.error("KoreaInvestAPI 인스턴스 초기화 실패. 종료합니다.")
        sys.exit(1)
    
    # WebSocket URL 설정
    ws_url = settings.KI_WEBSOCKET_URL
    logger.info(f"WebSocket URL: {ws_url}")
    
    # Queue 설정
    ws_req_queue = multiprocessing.Queue(maxsize=500)
    ws_result_queue = multiprocessing.Queue(maxsize=2000)
    
    logger.info("Queue 초기화 완료")
    
    # WebSocket 프로세스 시작
    logger.info("WebSocket 연결 시작...")
    run_websocket(korea_invest_service.api_instance, ws_url, ws_req_queue, ws_result_queue)
```

### 3. 호가 데이터 파싱 문제 해결

#### A. 데이터 형식 문제
- **문제**: KIS API WebSocket 데이터가 JSON이 아닌 `^` 구분 형식으로 전송
- **해결**: `receive_realtime_hoga_domestic_new()` 함수 구현

**파일**: `/home/wide/projects/systrading/backend/app/domestic_websocket.py`

```python
def receive_realtime_hoga_domestic_new(data):
    """
    KIS API H0STASP0 실시간 호가 데이터 파싱 (^ 구분 형식)
    데이터 형식: 종목코드^시간^현재가^매도1호가^매도2호가^...^매도10호가^매수1호가^매수2호가^...^매수10호가^매도1수량^...^매도10수량^매수1수량^...^매수10수량
    """
    try:
        values = data.split('^')
        
        if len(values) < 41:  # 최소 필요한 필드 수 확인
            logger.warning(f"호가 데이터 길이 부족: {len(values)}, 데이터: {data[:100]}")
            return None
        
        stock_code = values[0]  # 종목코드
        time_str = values[1]    # 시간 (HHMMSS)
        
        # 현재가 계산: 3번째 필드가 0이면 매도1호가와 매수1호가의 중간값으로 계산
        if len(values) >= 14:
            ask1_price = int(values[3]) if values[3] != '0' else 0
            bid1_price = int(values[13]) if values[13] != '0' else 0
            if ask1_price > 0 and bid1_price > 0:
                current_price = (ask1_price + bid1_price) // 2
            else:
                current_price = int(values[2]) if values[2] != '0' else 0
        else:
            current_price = int(values[2]) if values[2] != '0' else 0
        
        # 매도호가 (askp1~askp10): 인덱스 3~12
        asks = []
        for i in range(10):
            price_idx = 3 + i
            qty_idx = 23 + i  # askp_rsqn1~askp_rsqn10: 인덱스 23~32
            price = int(values[price_idx]) if values[price_idx] != '0' else 0
            quantity = int(values[qty_idx]) if values[qty_idx] != '0' else 0
            asks.append({
                "price": price,
                "quantity": quantity,
                "order_count": 0
            })
        
        # 매수호가 (bidp1~bidp10): 인덱스 13~22
        bids = []
        for i in range(10):
            price_idx = 13 + i
            qty_idx = 33 + i  # bidp_rsqn1~bidp_rsqn10: 인덱스 33~42
            price = int(values[price_idx]) if values[price_idx] != '0' else 0
            quantity = int(values[qty_idx]) if values[qty_idx] != '0' else 0
            bids.append({
                "price": price,
                "quantity": quantity,
                "order_count": 0
            })
        
        # 타임스탬프 생성
        from datetime import datetime
        date_str = datetime.now().strftime("%Y%m%d")
        timestamp = f"{date_str[:4]}-{date_str[4:6]}-{date_str[6:8]}T{time_str[:2]}:{time_str[2:4]}:{time_str[4:6]}"
        
        return {
            "stock_code": stock_code,
            "asks": asks,
            "bids": bids,
            "current_price": current_price,
            "timestamp": timestamp,
            "market_data": {
                "open": 0,
                "high": 0,
                "low": 0,
                "volume": 0,
                "value": 0,
                "sign": "3",
                "change": 0,
                "drate": 0.0
            }
        }
    except Exception as e:
        logger.error(f"호가 ^ 데이터 파싱 오류: {e}, 데이터: {data[:100]}")
        return None
```

### 4. 호가 구독 등록 과정

#### A. 구독 등록 API
**엔드포인트**: `POST /api/realtime/subscribe/orderbook?stock_code=005930`

**파일**: `/home/wide/projects/systrading/backend/app/api/realtime.py`

```python
@router.post("/subscribe/orderbook")
async def subscribe_orderbook(
    stock_code: str,
    realtime_service: RealtimeDataService = Depends(get_realtime_service)
):
    """
    특정 종목의 호가 데이터 구독 시작
    """
    import queue
    try:
        from app.main import ws_req_queue
        
        logger.info(f"호가 구독 요청: {stock_code}")
        
        if ws_req_queue is None:
            logger.error("ws_req_queue가 초기화되지 않음")
            raise HTTPException(status_code=503, detail="WebSocket 서비스 초기화 중")
        
        logger.info(f"호가 구독 요청: {stock_code}, Queue 크기: {ws_req_queue.qsize()}")
        
        # Queue에 구독 요청 추가
        ws_req_queue.put({
            "action_id": "실시간호가등록",
            "종목코드": stock_code
        }, timeout=1.0)
        
        logger.info(f"호가 구독 Queue 추가 성공: {stock_code}")
        
        return {
            "success": True,
            "message": f"종목 {stock_code} 호가 구독 시작",
            "stock_code": stock_code
        }
    except queue.Full:
        logger.error(f"호가 구독 Queue 가득 참: {stock_code}")
        raise HTTPException(status_code=503, detail="요청 대기열이 가득 찼습니다. 잠시 후 다시 시도하세요.")
    except Exception as e:
        logger.error(f"호가 구독 실패: {stock_code}, 에러: {str(e)}, 타입: {type(e).__name__}")
        raise HTTPException(status_code=500, detail=f"호가 구독 실패: {str(e)}")
```

#### B. WebSocket에서 구독 요청 처리
**파일**: `/home/wide/projects/systrading/backend/app/domestic_websocket.py`

```python
while True:
    if not ws_req_queue.empty():
        req_data = ws_req_queue.get()
        action_id = req_data['action_id']
        stock_code = req_data.get('종목코드')
        
        logger.info(f"[DEBUG] Queue에서 요청 수신: action_id={action_id}, stock_code={stock_code}")

        if action_id == "실시간호가등록":
            logger.info(f"실시간호가등록 {stock_code}")
            send_data = korea_invest_api.get_send_data(cmd=1, stock_code=stock_code)
            logger.info(f"[DEBUG] 호가 구독 데이터 전송: {send_data[:100]}...")
            await websocket.send(send_data)
            logger.info(f"[DEBUG] 호가 구독 데이터 전송 완료")
```

### 5. 프론트엔드 WebSocket 연결 수정

#### A. WebSocket 메시지 처리 수정
**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/websocket.ts`

```typescript
export function subscribeToOrderBookUpdates(
  stockCode: string,
  callback: (data: OrderBookData) => void
): () => void {
  const listener = (data: any) => {
    // data는 이미 notifyListeners에서 전달된 데이터
    // 백엔드에서 보내는 메시지 구조: { stock_code, data: { asks, bids, current_price, timestamp } }
    if (data && data.stock_code === stockCode) {
      console.log(`📊 호가 데이터 수신: ${stockCode}`, data);
      callback(data.data as OrderBookData);
    }
  };

  wsManager.subscribe('orderbook_update', listener);

  return () => {
    wsManager.unsubscribe('orderbook_update', listener);
  };
}
```

#### B. WebSocket 연결 자동 시작
**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/hooks/useOrderBook.ts`

```typescript
// WebSocket 연결 및 메시지 구독
useEffect(() => {
  // WebSocket 연결 확인 및 시작
  if (!wsManager.isConnected()) {
    console.log('🔌 WebSocket 연결 시작...');
    wsManager.connect().catch((error) => {
      console.error('WebSocket 연결 실패:', error);
      setError('WebSocket 연결 실패');
    });
  }

  const unsubscribeWs = subscribeToOrderBookUpdates(stockCode, (data) => {
    console.log(`📊 호가 데이터 업데이트: ${stockCode}`, data);
    setOrderBook(data);
    setError(null);
  });

  return unsubscribeWs;
}, [stockCode]);
```

#### C. 디버그 로그 추가
**파일**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/websocket.ts`

```typescript
private handleMessage(message: RealtimeMessage): void {
  const { type, data } = message;
  
  // 디버그: 모든 메시지 로그
  console.log('🔍 WebSocket 메시지 수신:', { type, data });

  switch (type) {
    case 'orderbook_update':
      console.log('📊 호가 업데이트 메시지 수신:', data);
      this.notifyListeners(type, data);
      break;
    // ... 기타 case들
  }
}
```

## 🎯 현재 상태 (2025-10-15 10:37 기준)

### ✅ 성공한 부분
1. **WebSocket 초기화**: KIS API WebSocket 연결 성공
2. **호가 구독 등록**: API 요청 성공
3. **데이터 파싱**: `^` 구분 형식 파싱 성공
4. **백엔드 데이터 수신**: 실제 호가 데이터 수신 확인
5. **캐시 저장**: RealtimeDataService에서 데이터 캐싱 성공

### 🔄 확인 중인 부분
1. **프론트엔드 WebSocket 연결**: 브라우저에서 연결 상태 확인 필요
2. **실시간 데이터 전달**: 백엔드 → 프론트엔드 데이터 전달 확인 필요
3. **호가창 렌더링**: 실제 데이터가 화면에 표시되는지 확인 필요

### 📊 최근 테스트 결과
```bash
# 백엔드 API 테스트 (성공)
curl "http://localhost:8000/api/realtime/orderbook/005930"
# 결과: current_price: 93,150, asks/bids 실제 데이터 수신

# 호가 구독 등록 테스트 (성공)
curl -X POST "http://localhost:8000/api/realtime/subscribe/orderbook?stock_code=005930"
# 결과: {"success":true,"message":"종목 005930 호가 구독 시작","stock_code":"005930"}
```

## 🔧 Chrome MCP 설정

### A. MCP 설정 파일 업데이트
**파일**: `/home/wide/.cursor/mcp.json`

```json
{
  "mcpServers": {
    "chrome": {
      "command": "npx",
      "args": ["chrome-devtools-mcp@latest"],
      "env": {}
    }
  }
}
```

### B. Chrome 브라우저 디버깅 모드
```bash
google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug --new-window http://localhost:9000/trview?stock_code=005930 &
```

### C. Chrome DevTools MCP 서버
```bash
npx chrome-devtools-mcp@latest &
```

**상태**: Chrome MCP 도구는 Cursor IDE 재시작 후 사용 가능 예상

## 📋 다음 단계

### 1. 즉시 확인 필요사항
1. **브라우저 콘솔 로그 확인**:
   - `🔌 WebSocket 연결 시작...`
   - `WebSocket 연결됨`
   - `🔍 WebSocket 메시지 수신:`
   - `📊 호가 업데이트 메시지 수신:`

2. **브라우저 Network 탭 확인**:
   - WebSocket 연결: `ws://localhost:8000/ws`
   - API 호출: `/api/realtime/subscribe/orderbook?stock_code=005930`

### 2. 문제 해결 방향
1. **WebSocket 연결 실패 시**: CORS 설정, 백엔드 서버 상태 확인
2. **메시지 수신 실패 시**: 메시지 구조 불일치 확인
3. **데이터 파싱 실패 시**: 프론트엔드 데이터 처리 로직 확인

### 3. 디버깅 명령어
```bash
# 백엔드 서버 상태 확인
ps aux | grep uvicorn

# WebSocket 프로세스 상태 확인  
ps aux | grep domestic_websocket

# 프론트엔드 서버 상태 확인
ps aux | grep next

# Chrome 브라우저 상태 확인
ps aux | grep chrome

# Chrome DevTools MCP 서버 상태 확인
ps aux | grep chrome-devtools-mcp
```

## 🔍 핵심 파일 위치

### 백엔드
- **WebSocket 처리**: `/home/wide/projects/systrading/backend/app/domestic_websocket.py`
- **API 엔드포인트**: `/home/wide/projects/systrading/backend/app/api/realtime.py`
- **실시간 서비스**: `/home/wide/projects/systrading/backend/app/services/realtime_service.py`
- **설정**: `/home/wide/projects/systrading/backend/app/core/config.py`

### 프론트엔드
- **WebSocket 관리**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/websocket.ts`
- **호가 훅**: `/home/wide/projects/systrading/stock-trading-ui/src/hooks/useOrderBook.ts`
- **호가 컴포넌트**: `/home/wide/projects/systrading/stock-trading-ui/src/components/trading/OrderBook.tsx`
- **API 클라이언트**: `/home/wide/projects/systrading/stock-trading-ui/src/lib/api-client.ts`

### 설정 파일
- **MCP 설정**: `/home/wide/.cursor/mcp.json`
- **백엔드 설정**: `/home/wide/projects/systrading/backend/config.yaml`

## 📝 참고사항

1. **장시간 작업**: WebSocket 토큰이 만료될 수 있으므로 주기적으로 확인 필요
2. **시장 시간**: 현재 장외 시간이므로 실제 호가 데이터가 제한적일 수 있음
3. **디버그 로그**: 프론트엔드에 상세한 디버그 로그가 추가되어 있어 브라우저 콘솔에서 확인 가능
4. **Chrome MCP**: Cursor IDE 재시작 후 Chrome MCP 도구 사용 가능 예상

---

**작성자**: Claude (Cursor AI Assistant)  
**작성일**: 2025-10-15 10:37 KST  
**작업 기간**: 약 1시간 30분  
**다음 작업**: 프론트엔드 WebSocket 연결 및 데이터 수신 확인

